# 管理員使用者重設密碼之流程

> 管理員「重設密碼」不是由管理員直接設定新密碼，而是觸發一個流程讓使用者自行設定新密碼。


## 流程

```
1. 管理員在後台對目標使用者點擊「重設密碼」

2. 系統執行：
   a. 凍結該使用者的登入狀態（禁用 / 取消驗證）
   b. 產生一組限時專用 token（綁定該使用者）
   c. 寄送 email 給使用者，內含專用重設密碼頁面網址（含 token）

3. 使用者收到 email，點擊連結進入專用頁面

4. 使用者在專用頁面輸入：
   - 原本帳號（account）
   - 原本密碼（舊密碼）
   - 新密碼

5. 後端驗證：
   a. 驗證 token 有效性（是否過期、是否已使用）
   b. 驗證帳號 + 舊密碼是否正確
   c. 以 checkUserPassword(lang, newPassword, { account }) 驗證新密碼（完整 14 項）
   d. 全部通過 → 儲存新密碼（加鹽雜湊）、恢復使用者登入狀態、標記 token 已使用

6. 使用者以新密碼重新登入
```


## 設計要點

- **管理員無法得知使用者密碼** — 管理員只能觸發重設流程，無法查看或設定使用者的密碼
- **使用者必須知道舊密碼** — 防止非本人透過 email 存取重設密碼（email 被盜時的第二道防線）
- **新密碼經完整驗證** — 後端從 token 查出 account，呼叫 `checkUserPassword` 做完整 14 項密碼規格檢查
- **token 限時且一次性** — 過期或已使用的 token 不可再用
- **帳號凍結期間無法登入** — 確保在使用者完成重設前，舊密碼也無法登入
