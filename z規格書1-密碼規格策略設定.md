# 規格書 1：密碼規格策略設定

> 依賴：無（最底層基礎設施）
> 被依賴：規格書 2、4、5、7


## 一、目的

透過 `settings.json` 配置的 `passwordPolicy` 設定區塊，將參數傳入 `wsemi.isUserPw` 進行密碼驗證，使不同公司可依需求自訂密碼規格。伺服器啟動時必須檢查此設定的完整性與合理性，若缺少或不合法則報錯阻止啟動。

> **注意**：`isUserPw` 的參數名稱與 `passwordPolicy` 欄位名稱不同（如 `numLenMin` vs `minLength`），由 `checkUserPassword` 負責轉換對應。

> **擴充**：後續規格書（如密碼過期、管理員重設等）若需新增欄位（如 `expireDays`），直接擴充 `passwordPolicy` 區塊並在 `WWebSso.mjs` 啟動檢查中加入對應驗證即可。


## 二、業主需求對照

以下為典型業主密碼規格需求及本設定的對應方式：

```
業主需求：密碼長度需在8~16碼，且須符合以下規則：
(1) 有0~9的數字
(2) 有英文字母(不限大小寫)
(3) 有特殊字元(! @ # $ ^ & * ( ) - + < > ? …等)
(4) 不可和駭客最常猜測複雜密碼相同(例如1qaz@WSX、P@ssw0rd等)
(5) 不可包含與自己姓名代號相同之2個以上之連續字元
(6) 不可包含「\」
```

| 業主需求 | 對應設定欄位 |
|----------|-------------|
| (1) 含數字 | `requireDigit: true` |
| (2) 含英文字母（不限大小寫） | `requireLetter: true`（任一大或小寫即可） |
| (3) 含特殊字元 | `requireSpecial: true` |
| (4) 禁止常見駭客密碼 | `commonPasswordBlacklist: [...]` |
| (5) 禁止與姓名代號連續字元 | `noConsecutiveCharsFromAccount: true`, `consecutiveCharsMinMatch: 2` |
| (6) 禁止反斜線 | `forbiddenChars: ['\\']` |
| 長度 8~16 | `minLength: 8`, `maxLength: 16` |
| 禁止空白 | `noSpace: true` |
| 僅允許 ASCII 可印刷字元 | `onlyAscii: true` |


## 三、設定格式設計

### settings.json 新增區塊

```javascript
passwordPolicy: {

    // === 長度限制 ===
    minLength: 8,              // 最小長度，正整數，>=1（對應 isUserPw 的 numLenMin）
    maxLength: 16,             // 最大長度，正整數，>=minLength（對應 isUserPw 的 numLenMax）

    // === 字元要求 ===
    requireLetter: true,       // 是否需包含英文字母（不分大小寫，大或小寫皆可），isUserPw 預設 false
    requireUppercase: false,   // 是否需包含大寫英文（與 requireLetter 獨立，可同時啟用），isUserPw 預設 true
    requireLowercase: false,   // 是否需包含小寫英文（與 requireLetter 獨立，可同時啟用），isUserPw 預設 true
    requireDigit: true,        // 是否需包含數字，isUserPw 預設 true
    requireSpecial: true,      // 是否需包含特殊符號，isUserPw 預設 true

    // === 禁止字元 ===
    noSpace: true,             // 是否禁止空白字元，isUserPw 預設 true
    onlyAscii: true,           // 是否限制僅可使用 ASCII 可印刷字元（擋中文、emoji、Unicode），isUserPw 預設 true
    forbiddenChars: ['\\'],    // 禁止包含的特定字元列表

    // === 帳號關聯檢查 ===
    noConsecutiveCharsFromAccount: true,  // 是否禁止密碼包含與帳號(account)相同的連續字元
    consecutiveCharsMinMatch: 2,          // 連續字元最小比對長度（>=2），業主需求為 2（isUserPw 預設 3，由此設定覆蓋）

    // === 黑名單 ===
    commonPasswordBlacklist: [            // 禁止使用的常見弱密碼清單（比對不分大小寫）
        // 數字序列
        '1234',
        '12345',
        '123456',
        '1234567',
        '12345678',
        '123456789',
        '1234567890',
        '12345678910',
        // 重複數字
        '111111',
        '000000',
        '666666',
        '888888',
        '7777777',
        '11111111',
        // 數字序列變體
        '123123',
        '123321',
        '654321',
        '987654321',
        // 鍵盤序列
        'qwerty',
        'qwerty123',
        'qwerty1',
        'qwertyuiop',
        'qazwsx',
        '1q2w3e4r',
        '1q2w3e4r5t',
        '1q2w3e',
        '1qaz2wsx',
        'zxcvbnm',
        // 常見單字
        'password',
        'password1',
        'passw0rd',
        'admin',
        'admin123',
        'welcome',
        'letmein',
        'login',
        'master',
        'monkey',
        'dragon',
        'shadow',
        'sunshine',
        'superman',
        'batman',
        'football',
        'baseball',
        'soccer',
        'hockey',
        'michael',
        'jordan',
        'jennifer',
        'hunter',
        'ranger',
        'harley',
        'thomas',
        'charlie',
        'andrew',
        'daniel',
        'ashley',
        'bailey',
        'mustang',
        'access',
        'secret',
        'ninja',
        'jesus',
        'hello',
        'freedom',
        'trustno1',
        // 帶特殊符號的常見弱密碼
        '1qaz@WSX',
        'P@ssw0rd',
        'Pass@123',
        'Aa123456',
        'Aa@123456',
        'Admin@123',
        'abc@123',
        // 字母序列
        'abc123',
        'abcdef',
        'abcd1234',
        'abc',
        // 常見片語
        'iloveyou',
        'fuckyou',
        'f*ckyou',
        'whatever',
        'nothing',
        'google',
        'computer',
        'internet',
        'samsung',
        'apple',
        'killer',
        'princess',
        'lovely',
        'tigger',
        'starwars',
        'pokemon',
        'minecraft',
        '123qwe',
        'zaq1zaq1',
        'photoshop',
        'adobe123',
        '18atcskd2w',
        'mynoob',
    ],

},
```

### 各欄位說明

> 以下「建議值」為上方業主設定範例所用的值。所有欄位皆為必填（啟動檢查缺少即 throw），無靜默降級。

| 欄位 | 類型 | 建議值 | isUserPw 對應參數 | 驗證規則 |
|------|------|--------|-------------------|----------|
| `minLength` | Integer | 8 | `numLenMin` | >=1 |
| `maxLength` | Integer | 16 | `numLenMax` | >=minLength |
| `requireLetter` | Boolean | true | `requireLetter`（isUserPw 預設 false） | — |
| `requireUppercase` | Boolean | false | `requireUppercase`（isUserPw 預設 true） | — |
| `requireLowercase` | Boolean | false | `requireLowercase`（isUserPw 預設 true） | — |
| `requireDigit` | Boolean | true | `requireDigit` | — |
| `requireSpecial` | Boolean | true | `requireSpecial` | — |
| `noSpace` | Boolean | true | `noSpace` | — |
| `onlyAscii` | Boolean | true | `onlyAscii` | — |
| `forbiddenChars` | Array\<String\> | `['\\']` | `forbiddenChars` | 每個元素為單一字元字串；空陣列表示不禁止 |
| `noConsecutiveCharsFromAccount` | Boolean | true | `noConsecutiveCharsFromAccount` | — |
| `consecutiveCharsMinMatch` | Integer | 2 | `consecutiveCharsMinMatch`（isUserPw 預設 3，由此設定覆蓋） | >=2，僅在 noConsecutiveCharsFromAccount=true 時生效 |
| `commonPasswordBlacklist` | Array\<String\> | 見上方完整清單 | `commonPasswordBlacklist` | 比對時不分大小寫；傳入空陣列可關閉黑名單檢查 |

### 字元要求的組合邏輯

`requireLetter`、`requireUppercase`、`requireLowercase` 三者為獨立設定：

| requireLetter | requireUppercase | requireLowercase | 效果 |
|:---:|:---:|:---:|------|
| true | false | false | 只要有英文字母即可（大或小寫皆可） |
| false | true | true | 必須同時有大寫和小寫 |
| true | true | false | 至少有英文字母，且必須有大寫 |
| false | false | false | 不要求英文字母 |

業主需求「有英文字母(不限大小寫)」對應：`requireLetter: true, requireUppercase: false, requireLowercase: false`

### isUserPw 驗證順序

`isUserPw` 內部驗證按以下順序執行，每一步失敗時產生對應的 error key：

| 順序 | 檢查項目 | Error Key |
|:---:|----------|-----------|
| 1 | 是否為有效字串 | `keyInvalidPassword` |
| 2 | numLenMin <= numLenMax 設定一致性 | `keyLimNumLenMinOrMax` |
| 3 | 禁止空白字元（noSpace） | `keyLimHasSpace` |
| 4 | 僅允許 ASCII（onlyAscii） | `keyLimNonAsciiChar` |
| 5 | 最小長度 | `keyLimNumLenMin` |
| 6 | 最大長度 | `keyLimNumLenMax` |
| 7 | 需包含英文字母（requireLetter） | `keyLimRequireLetter` |
| 8 | 需包含大寫（requireUppercase） | `keyLimRequireUppercase` |
| 9 | 需包含小寫（requireLowercase） | `keyLimRequireLowercase` |
| 10 | 需包含數字（requireDigit） | `keyLimRequireDigit` |
| 11 | 需包含特殊符號（requireSpecial） | `keyLimRequireSpecial` |
| 12 | 禁止字元（forbiddenChars） | `keyLimForbiddenChar` |
| 13 | 常見弱密碼黑名單 | `keyLimCommonPassword` |
| 14 | 帳號連續字元 | `keyLimConsecutiveCharsFromAccount` |

- `useOnlyOneError: true` 時，遇到第一個錯誤即 throw
- `useOnlyOneError: false` 時，收集所有錯誤以 ` | ` 分隔 throw
- `useKeyForError: true` 回傳 error key（如 `keyLimRequireLetter`），`false` 回傳中文訊息


## 四、帳號連續字元檢查邏輯

### 需求說明

> 不可包含與自己姓名代號相同之2個以上之連續字元
> 例如姓名代號 123456，密碼不可包含 12、23、34、45、56

### 檢查邏輯

```
輸入：password, account, consecutiveCharsMinMatch (N)

1. 將 account 與 password 皆轉為小寫（不分大小寫比對）
2. 從 account 中取出所有長度為 N 的連續子字串：
   例如 account = '123456', N = 2
   子字串 = ['12', '23', '34', '45', '56']
3. 檢查 password（小寫）是否包含任何一個子字串
4. 若包含 → 驗證失敗
```

### 注意事項

- **後端必須傳入 account**：所有後端呼叫 `checkUserPassword` 的場景，呼叫端必須負責提供 account，確保完整 14 項驗證皆執行：
  ```javascript
  checkUserPassword(lang, pw, { account: 'xxx' })
  ```
- **各場景 account 來源**：

  | 場景 | account 來源 |
  |------|-------------|
  | 使用者改自己密碼 | 後端從 token 查 user 取得 |
  | 管理員創建新使用者 | 前端表單同時送出 account 和 password，後端從請求中取得 |
  | 管理員重設密碼 | 後端已知目標使用者 |

- **前端即時驗證（非安全邊界）**：前端在使用者輸入密碼時可做即時預覽驗證，此時 account 可能為空，`isUserPw` 會自動跳過連續字元檢查，僅跑其他 13 項。這只是 UX 輔助，不影響安全性，因為最終送出時後端會做完整 14 項驗證


## 五、常見弱密碼黑名單檢查邏輯

### 檢查邏輯

```
1. 將 password 轉為小寫
2. 將 commonPasswordBlacklist 中每一項也轉為小寫
3. 完全比對（不是包含，是完全相同）
4. 若匹配 → 驗證失敗
```

### 黑名單維護

- 黑名單定義在 `settings.json` 中，管理員可自行增減
- 建議提供一份預設清單作為參考（見上方設定範例）
- 比對不分大小寫，避免 `P@ssw0rd` 與 `p@ssw0rd` 需要重複列舉


## 六、ASCII 限制與禁止字元檢查邏輯

### onlyAscii 檢查邏輯

```
1. 若 onlyAscii = true，使用正則 /[^\x20-\x7e]/ 檢查 password
2. ASCII printable 範圍：0x20(space) ~ 0x7E(~)
3. 中文、emoji、Unicode 字元皆會被擋掉
4. 若包含非 ASCII 字元 → 驗證失敗（error key: keyLimNonAsciiChar）
```

注意：tab/newline 等控制字元同時被 `noSpace` 和 `onlyAscii` 涵蓋，`useOnlyOneError=false` 時可能重複報錯，屬預期行為。

### forbiddenChars 檢查邏輯

```
1. 逐字元檢查 password 是否包含 forbiddenChars 中的任何字元
2. 若包含 → 驗證失敗（error key: keyLimForbiddenChar）
3. useKeyForError 模式下只報一次（遇到第一個禁止字元即 break）
```

### 注意事項

- `forbiddenChars` 為必填，空陣列合法（表示不禁止任何特定字元）
- JSON 中反斜線需跳脫：`['\\']` 代表禁止 `\`


## 七、伺服器啟動檢查

### 位置：WWebSso.mjs（接續現有設定讀取區塊，約 line 232 之後）

### 檢查邏輯

```
1. 讀取 opt.passwordPolicy
2. 若不存在或非物件 → throw new Error('invalid passwordPolicy: missing')
3. 逐欄位檢查（所有欄位皆為必填，缺少即報錯）：
   a. minLength：必須為正整數且 >=1，否則報錯
   b. maxLength：必須為正整數且 >= minLength，否則報錯
   c. requireLetter/requireUppercase/requireLowercase/requireDigit/requireSpecial/noSpace/onlyAscii：
      必須為布林值，否則報錯
   d. forbiddenChars：必須為陣列，每個元素必須為長度 1 的字串，否則報錯（空陣列合法）
   e. noConsecutiveCharsFromAccount：必須為布林值，否則報錯
   f. consecutiveCharsMinMatch：若 noConsecutiveCharsFromAccount=true，
      必須為正整數且 >=2，否則報錯
   g. commonPasswordBlacklist：必須為字串陣列，否則報錯（空陣列合法，表示關閉黑名單）
4. 所有檢查通過後，將 passwordPolicy 物件傳入 procCore
```

### 錯誤處理風格

參考現有 WOrm/url/db 的嚴格檢查風格（throw Error），而非現有設定的靜默替換預設值風格。因為密碼策略屬安全性關鍵設定，不應靜默降級：

```javascript
//passwordPolicy
let passwordPolicy = get(opt, 'passwordPolicy')
if (!iseobj(passwordPolicy)) {
    throw new Error('invalid passwordPolicy: missing or not an object')
}
let ppMinLength = get(passwordPolicy, 'minLength')
if (!ispint(ppMinLength) || cint(ppMinLength) < 1) {
    throw new Error('invalid passwordPolicy.minLength: must be a positive integer >= 1')
}
let ppMaxLength = get(passwordPolicy, 'maxLength')
if (!ispint(ppMaxLength) || cint(ppMaxLength) < cint(ppMinLength)) {
    throw new Error('invalid passwordPolicy.maxLength: must be >= minLength')
}
// ... 以此類推
```


## 八、傳遞路徑

```
settings.json
  → WWebSso.mjs（讀取 + 啟動檢查）
    → procCore(woItems, procOrm, { ..., passwordPolicy })
      → checkUserPassword(lang, pw, opt) 改用 passwordPolicy
      → checkTokenAndChangePassword 改用 passwordPolicy
```

### procCore 需調整

現有 `checkUserPassword` 呼叫 `isUserPw(pw, { useKeyForError: true, useOnlyOneError: true })`，需改為將 `passwordPolicy` 的欄位轉換為 `isUserPw` 的參數名稱傳入。

`checkUserPassword` 的簽名需擴充：
```javascript
// 現有
let checkUserPassword = (lang, pw) => { ... }

// 改為（支援帳號連續字元檢查）
let checkUserPassword = (lang, pw, opt = {}) => {
    // opt.account — 用於 noConsecutiveCharsFromAccount 檢查
    ...
}
```

### passwordPolicy → isUserPw 參數對應

`checkUserPassword` 內呼叫 `isUserPw` 時需進行欄位名稱轉換：

```javascript
isUserPw(pw, {
    useKeyForError: true,
    useOnlyOneError: true,
    numLenMin: passwordPolicy.minLength,        // minLength → numLenMin
    numLenMax: passwordPolicy.maxLength,        // maxLength → numLenMax
    requireLetter: passwordPolicy.requireLetter,
    requireUppercase: passwordPolicy.requireUppercase,
    requireLowercase: passwordPolicy.requireLowercase,
    requireDigit: passwordPolicy.requireDigit,
    requireSpecial: passwordPolicy.requireSpecial,
    noSpace: passwordPolicy.noSpace,
    onlyAscii: passwordPolicy.onlyAscii,
    forbiddenChars: passwordPolicy.forbiddenChars,
    commonPasswordBlacklist: passwordPolicy.commonPasswordBlacklist,
    account: opt.account || '',
    noConsecutiveCharsFromAccount: passwordPolicy.noConsecutiveCharsFromAccount,
    consecutiveCharsMinMatch: passwordPolicy.consecutiveCharsMinMatch,
})
```


## 九、前端傳遞

密碼規格相關資訊（minLength、maxLength、字元要求）可視需要透過 `webInfor` 傳至前端，供前端在密碼輸入時即時提示規則，但非必要（後端驗證為主）。


## 十、設定變化範例

> section 三的完整設定範例即為業主需求的建議設定。以下僅列出不同場景的差異欄位：

### 場景 A：不要求特殊字元、不限最大長度

```javascript
requireSpecial: false,
maxLength: 128,
```

### 場景 B：要求大小寫分別存在

```javascript
requireLetter: false,          // 由 requireUppercase + requireLowercase 取代
requireUppercase: true,
requireLowercase: true,
```

### 場景 C：關閉黑名單檢查

```javascript
commonPasswordBlacklist: [],   // 空陣列即關閉
```

## 十一、影響範圍

| 檔案 | 變動 |
|------|------|
| `settings.json` | 新增 `passwordPolicy` 區塊 |
| `WWebSso.mjs` | 新增啟動檢查邏輯、將 passwordPolicy 傳入 procCore |
| `procCore.mjs` | `checkUserPassword` 改用 passwordPolicy 驗證，簽名擴充支援 account |
| `procLang.mjs` | 驗證錯誤訊息需支援動態數值及新增錯誤類型 |

### procLang.mjs 完整錯誤語系鍵

以下為 `isUserPw` 所有 error key（`useKeyForError: true` 時回傳的 key），對應 `procLang.mjs` 中需存在的語系鍵（前綴 `userPassword_`）：

**已存在的語系鍵（需修改為支援動態數值）：**

> 因為 `minLength`、`maxLength` 由 `passwordPolicy` 配置，語系訊息不可寫死數字。
> `checkUserPassword` 內取得 `keyErr` 後，需以 `passwordPolicy` 的值替換模板變數再回傳。

```javascript
userPassword_keyLimNumLenMinOrMax: {
    eng: 'The minimum password length is greater than the maximum length',
    cht: '設定密碼最小長度大於最大長度',
},
userPassword_keyLimHasSpace: {
    eng: 'Password must not contain whitespace characters',
    cht: '密碼不可包含空白字元',
},
userPassword_keyLimNumLenMin: {
    eng: 'Password length must be at least {minLength} characters',       // 原本寫死 8，改為模板
    cht: '密碼長度須大於等於{minLength}個字元',                              // 原本寫死 8，改為模板
},
userPassword_keyLimNumLenMax: {
    eng: 'Password length must not exceed {maxLength} characters',        // 原本寫死 30，改為模板
    cht: '密碼長度須小於等於{maxLength}個字元',                              // 原本寫死 30，改為模板
},
```

**需新增的語系鍵：**

```javascript
userPassword_keyInvalidPassword: {
    eng: 'Invalid password',
    cht: '密碼非有效字串',
},
userPassword_keyLimNonAsciiChar: {
    eng: 'Password may only contain ASCII printable characters (letters, digits, symbols)',
    cht: '密碼僅允許使用英文大小寫、數字及特殊符號',
},
userPassword_keyLimRequireLetter: {
    eng: 'Password must contain at least one letter',
    cht: '密碼須包含至少一個英文字母',
},
userPassword_keyLimRequireUppercase: {
    eng: 'Password must contain at least one uppercase letter',
    cht: '密碼須包含至少一個大寫英文字母',
},
userPassword_keyLimRequireLowercase: {
    eng: 'Password must contain at least one lowercase letter',
    cht: '密碼須包含至少一個小寫英文字母',
},
userPassword_keyLimRequireDigit: {
    eng: 'Password must contain at least one digit',
    cht: '密碼須包含至少一個數字',
},
userPassword_keyLimRequireSpecial: {
    eng: 'Password must contain at least one special character',
    cht: '密碼須包含至少一個特殊符號',
},
userPassword_keyLimForbiddenChar: {
    eng: 'Password must not contain forbidden characters',
    cht: '密碼不可包含禁止字元',
},
userPassword_keyLimCommonPassword: {
    eng: 'This password is too common and not allowed',
    cht: '此密碼為常見弱密碼，不允許使用',
},
userPassword_keyLimConsecutiveCharsFromAccount: {
    eng: 'Password must not contain {consecutiveCharsMinMatch} or more consecutive characters from your account name',
    cht: '密碼不可包含與帳號相同之{consecutiveCharsMinMatch}個以上連續字元',
},
```

**需移除的舊語系鍵（已被上述個別 key 取代）：**

```javascript
// 移除：userPassword_keyLimCombination（舊版合併錯誤訊息，isUserPw 已改為個別 key）
```

### 模板變數替換邏輯

`checkUserPassword` 取得 `keyErr` 和語系訊息後，需替換模板變數：

```javascript
let msg = get(kpLang, `${lang}.userPassword_${keyErr}`, '')

// 替換模板變數
msg = msg.replace('{minLength}', passwordPolicy.minLength)
msg = msg.replace('{maxLength}', passwordPolicy.maxLength)
msg = msg.replace('{consecutiveCharsMinMatch}', passwordPolicy.consecutiveCharsMinMatch)
```


## 十二、測試矩陣

> 以下測試基於業主建議設定（section 三），除非個別測試項另行指定 policy。
> 測試用 policy 簡稱 **P0**：`minLength:8, maxLength:16, requireLetter:true, requireUppercase:false, requireLowercase:false, requireDigit:true, requireSpecial:true, noSpace:true, onlyAscii:true, forbiddenChars:['\\'], noConsecutiveCharsFromAccount:true, consecutiveCharsMinMatch:2, commonPasswordBlacklist:[完整清單]`


### A. 啟動檢查（WWebSso.mjs — passwordPolicy 設定驗證）

| # | 測試項目 | 輸入 | 預期結果 |
|---|---------|------|---------|
| A1 | 合法設定 | P0 完整設定 | 啟動成功 |
| A2 | passwordPolicy 缺少 | `opt` 中無 passwordPolicy | throw `invalid passwordPolicy: missing` |
| A3 | passwordPolicy 非物件 | `passwordPolicy: 'abc'` | throw `invalid passwordPolicy: missing` |
| A4 | minLength 缺少 | 移除 minLength | throw |
| A5 | minLength 為 0 | `minLength: 0` | throw（必須 >=1） |
| A6 | minLength 為負數 | `minLength: -1` | throw |
| A7 | minLength 非整數 | `minLength: 'abc'` | throw |
| A8 | maxLength < minLength | `minLength: 10, maxLength: 5` | throw |
| A9 | maxLength = minLength | `minLength: 8, maxLength: 8` | 啟動成功（合法） |
| A10 | requireLetter 非布林 | `requireLetter: 1` | throw |
| A11 | forbiddenChars 元素非單字元 | `forbiddenChars: ['ab']` | throw |
| A12 | forbiddenChars 空陣列 | `forbiddenChars: []` | 啟動成功 |
| A13 | consecutiveCharsMinMatch = 1 | `noConsecutiveCharsFromAccount: true, consecutiveCharsMinMatch: 1` | throw（必須 >=2） |
| A14 | noConsecutiveCharsFromAccount=false 時不檢查 consecutiveCharsMinMatch | `noConsecutiveCharsFromAccount: false` 且無 consecutiveCharsMinMatch | 啟動成功 |
| A15 | commonPasswordBlacklist 空陣列 | `commonPasswordBlacklist: []` | 啟動成功（合法，關閉黑名單） |
| A16 | commonPasswordBlacklist 含非字串 | `commonPasswordBlacklist: [123]` | throw |


### B. 密碼驗證（checkUserPassword — 每個 error key 至少一個 fail case）

> 以下皆使用 P0 設定，account 為 `'testuser'`（除非另行指定）。

#### B-1. 基礎驗證

| # | 測試項目 | 密碼 | account | 預期 | Error Key |
|---|---------|------|---------|------|-----------|
| B1 | 合法密碼 | `'Abcd@123'` | `'testuser'` | pass | — |
| B2 | 密碼為 null | `null` | `'testuser'` | fail | `keyInvalidPassword` |
| B3 | 密碼為數字型別 | `12345678` | `'testuser'` | fail | `keyInvalidPassword` |
| B4 | 密碼為空字串 | `''` | `'testuser'` | fail | `keyLimNumLenMin` |

#### B-2. 空白與 ASCII

| # | 測試項目 | 密碼 | 預期 | Error Key |
|---|---------|------|------|-----------|
| B5 | 含空白 | `'Abc @123'` | fail | `keyLimHasSpace` |
| B6 | 含 tab | `'Abc\t@123'` | fail | `keyLimHasSpace` |
| B7 | 含中文 | `'Abc@123密碼'` | fail | `keyLimNonAsciiChar` |
| B8 | 含 emoji | `'Abc@123😀'` | fail | `keyLimNonAsciiChar` |
| B9 | onlyAscii=false 時含中文 | `'Abc@123密碼'`（policy: `onlyAscii:false`） | pass（長度/字元符合即可） | — |

#### B-3. 長度邊界

| # | 測試項目 | 密碼 | 預期 | Error Key |
|---|---------|------|------|-----------|
| B10 | 剛好 minLength（8） | `'Ab@12345'`（8 字元） | pass | — |
| B11 | minLength - 1（7） | `'Ab@1234'`（7 字元） | fail | `keyLimNumLenMin` |
| B12 | 剛好 maxLength（16） | `'Ab@1234567890123'`（16 字元） | pass | — |
| B13 | maxLength + 1（17） | `'Ab@12345678901234'`（17 字元） | fail | `keyLimNumLenMax` |

#### B-4. 字元要求

| # | 測試項目 | 密碼 | 預期 | Error Key |
|---|---------|------|------|-----------|
| B14 | 無英文字母 | `'1234@678'` | fail | `keyLimRequireLetter` |
| B15 | 有英文（小寫即可） | `'abcd@123'` | pass | — |
| B16 | 有英文（大寫即可） | `'ABCD@123'` | pass | — |
| B17 | requireUppercase=true 但無大寫 | `'abcd@123'`（policy: `requireUppercase:true`） | fail | `keyLimRequireUppercase` |
| B18 | requireLowercase=true 但無小寫 | `'ABCD@123'`（policy: `requireLowercase:true`） | fail | `keyLimRequireLowercase` |
| B19 | 無數字 | `'Abcdefg@'` | fail | `keyLimRequireDigit` |
| B20 | 無特殊符號 | `'Abcd1234'` | fail | `keyLimRequireSpecial` |

#### B-5. 禁止字元

| # | 測試項目 | 密碼 | 預期 | Error Key |
|---|---------|------|------|-----------|
| B21 | 含反斜線 | `'Ab@1234\\'` | fail | `keyLimForbiddenChar` |
| B22 | 不含反斜線 | `'Ab@12345'` | pass | — |
| B23 | forbiddenChars=[] 時含反斜線 | `'Ab@1234\\'`（policy: `forbiddenChars:[]`） | pass | — |

#### B-6. 黑名單

| # | 測試項目 | 密碼 | 預期 | Error Key |
|---|---------|------|------|-----------|
| B24 | 完全匹配黑名單 | `'1qaz@WSX'` | fail | `keyLimCommonPassword` |
| B25 | 黑名單不分大小寫 | `'1QAZ@wsx'` | fail | `keyLimCommonPassword` |
| B26 | 黑名單為子字串但非完全匹配 | `'1qaz@WSXabc'` | pass（不是完全匹配） | — |
| B27 | 空黑名單 | `'1qaz@WSX'`（policy: `commonPasswordBlacklist:[]`） | pass | — |

#### B-7. 帳號連續字元

| # | 測試項目 | 密碼 | account | N | 預期 | Error Key |
|---|---------|------|---------|---|------|-----------|
| B28 | 含帳號連續 2 字元 | `'Ab@1te34'` | `'testuser'` | 2 | fail（含 `te`） | `keyLimConsecutiveCharsFromAccount` |
| B29 | 不含帳號連續 2 字元 | `'Ab@12345'` | `'testuser'` | 2 | pass | — |
| B30 | 大小寫不敏感 | `'Ab@1TE34'` | `'testuser'` | 2 | fail（`TE` 匹配 `te`） | `keyLimConsecutiveCharsFromAccount` |
| B31 | N=3 時 2 字元不觸發 | `'Ab@1te34'` | `'testuser'` | 3 | pass（`te` 長度 < 3） | — |
| B32 | N=3 時 3 字元觸發 | `'Ab@tes34'` | `'testuser'` | 3 | fail（含 `tes`） | `keyLimConsecutiveCharsFromAccount` |
| B33 | account 為空 | `'Ab@1te34'` | `''` | 2 | pass（跳過檢查） | — |
| B34 | account 長度 < N | `'Ab@1a234'` | `'a'` | 2 | pass（跳過檢查） | — |
| B35 | account 長度 = N | `'Ab@1ab34'` | `'ab'` | 2 | fail（含 `ab`） | `keyLimConsecutiveCharsFromAccount` |


### C. 語系與模板變數

| # | 測試項目 | lang | Error Key | 預期訊息（含替換後數值） |
|---|---------|------|-----------|----------------------|
| C1 | 最小長度 eng | `'eng'` | `keyLimNumLenMin` | `'Password length must be at least 8 characters'` |
| C2 | 最小長度 cht | `'cht'` | `keyLimNumLenMin` | `'密碼長度須大於等於8個字元'` |
| C3 | 最大長度 eng | `'eng'` | `keyLimNumLenMax` | `'Password length must not exceed 16 characters'` |
| C4 | 最大長度 cht | `'cht'` | `keyLimNumLenMax` | `'密碼長度須小於等於16個字元'` |
| C5 | 帳號連續 eng | `'eng'` | `keyLimConsecutiveCharsFromAccount` | `'Password must not contain 2 or more consecutive characters from your account name'` |
| C6 | 帳號連續 cht | `'cht'` | `keyLimConsecutiveCharsFromAccount` | `'密碼不可包含與帳號相同之2個以上連續字元'` |
| C7 | 其他 policy 數值 | `minLength:10, maxLength:20, consecutiveCharsMinMatch:3` | `keyLimNumLenMin` | `'Password length must be at least 10 characters'`（確認 10 非 8） |


### D. 完整場景驗證（對應 z密碼場景.md）

| # | 場景 | 測試重點 | 預期 |
|---|------|---------|------|
| D1 | 使用者註冊 / 管理員創建帳號 | `checkUserPassword(lang, pw, { account })` 傳入表單 account + password | 14 項完整驗證 |
| D2 | 使用者自行改密碼 | `checkTokenAndChangePassword` 內部從 token 取 account 後傳入 `checkUserPassword` | 14 項完整驗證 |
| D3 | 前端即時預覽（account 為空） | `checkUserPassword(lang, pw, { account: '' })` | 跑 13 項，跳過帳號連續字元 |
| D4 | 前端即時預覽（account 已填） | `checkUserPassword(lang, pw, { account: 'xxx' })` | 14 項完整驗證 |
| D5 | 合法密碼通過所有檢查 | `'Xk@93mz!'` + account `'admin01'` | pass，回傳 `{ state: 'success', msg: 'ok' }` |
| D6 | 不合法密碼回傳正確錯誤 | `'123'` + account `'admin01'` | fail，回傳 `{ state: 'error', msg: '...' }` 且 msg 為對應語系訊息 |
