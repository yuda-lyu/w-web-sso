# 使用者變更帳密流程

## 觸發

使用者於 user view（`viewState='user'`，PageUser.vue）點擊「變更密碼 / Change Password」按鈕，展開變更表單，輸入舊密碼、新密碼、確認密碼後點擊「送出 / Submit」。

## 重要流程

> 編號規則:
> - `**E2E-NNN**`: UI baseline 測試, 對應 `test/e2e-changepassword.test.mjs` + `test/pics/changepassword/changepassword-{lang}-E2E-NNN-*.png`
> - `**E2E-NNN(待補)**`: 編號預留, 尚無 e2e 待未來補

- **E2E-001** 使用者於使用者頁點擊變更密碼, 展開變更密碼表單（舊密碼/新密碼/確認密碼三欄位）
- **E2E-002** 使用者送出變更, 未填舊密碼, 於「舊密碼」欄位下方以紅字 inline 顯示對應提示
- **E2E-003** 使用者送出變更, 未填新密碼, 於「新密碼」欄位下方以紅字 inline 顯示對應提示
- **E2E-004** 使用者送出變更, 未填確認密碼, 於「確認密碼」欄位下方以紅字 inline 顯示對應提示
- **E2E-005** 使用者送出變更, 新密碼≠確認密碼, 於「確認密碼」欄位下方以紅字 inline 顯示不一致
- **E2E-006** 使用者送出變更, 新密碼不符密碼策略（長度/字元類/黑名單/與帳號連續字元等）, 於「新密碼」欄位下方以紅字 inline 顯示具體策略名
- **E2E-007** 使用者送出變更, 新密碼通過策略但舊密碼不符, 後端 reject, 於「舊密碼」欄位下方以紅字 inline 顯示統一「變更失敗」訊息（不洩露細節）
- **E2E-008** 使用者送出變更, 三欄都填妥+新密碼合規+舊密碼正確, 變更成功+寄通知信（成功訊息以 alert 通知）
- **E2E-009** 使用者送出變更, 前端密碼策略檢查（呼叫後端 checkUserPassword）發生網路錯誤, 於「新密碼」欄位下方以紅字 inline 顯示網路錯誤 (E2E-009-network-error, 用 page.route 攔截 /api/main 模擬)
- **E2E-010** 使用者送出變更, token 失效（如過期/被清除）, 後端 checkToken reject, 於「舊密碼」欄位下方以紅字 inline 顯示統一「變更失敗」訊息 (共用 E2E-007 baseline, 視覺等同)

## 執行流程

### 一、展開變更密碼表單

```
001  使用者點擊「變更密碼」按鈕  [PageUser.vue:367]
002  showChangePassword = true（展開表單）  [PageUser.vue:369]
003  清空 oldPassword / newPassword / confirmPassword 欄位  [PageUser.vue:370-372]
004  showOldPassword / showNewPassword / showConfirmPassword 預設 false（密碼遮罩）  [PageUser.vue:373-375]
005  清空 chPwOldError / chPwNewError / chPwConfirmError 三個 inline 錯誤訊息  [PageUser.vue:377-379]
```

### 二、取消變更

```
001  使用者點擊「取消」按鈕  [PageUser.vue:382]
002  showChangePassword = false（收起表單）  [PageUser.vue:384]
003  清空 chPwOldError / chPwNewError / chPwConfirmError  [PageUser.vue:386-388]
```

> 註：欄位內容不主動清空，使用者下次再點「變更密碼」時，clickChangePassword 會清空。

### 三、送出變更

```
001  使用者點擊「送出」按鈕，呼叫 submitChangePassword  [PageUser.vue:391]
002  $ui.updateLoading(true) 顯示 loading  [PageUser.vue:473]
003  執行 core() 非同步流程  [PageUser.vue:394]
004      每次送出先清空 chPwOldError / chPwNewError / chPwConfirmError 三個 inline 錯誤訊息  [PageUser.vue:397-399]
005      檢查 oldPassword 為空  [PageUser.vue:402]
006          空:
007              chPwOldError = $t('userChangePasswordForNoOldPassword')（舊密碼欄下方紅字）, return  [PageUser.vue:403]
008      檢查 newPassword 為空  [PageUser.vue:408]
009          空:
010              chPwNewError = $t('userChangePasswordForNoNewPassword'), return  [PageUser.vue:409]
011      檢查 confirmPassword 為空  [PageUser.vue:414]
012          空:
013              chPwConfirmError = $t('userChangePasswordForNoConfirmPassword'), return  [PageUser.vue:415]
014      檢查 newPassword 與 confirmPassword 一致  [PageUser.vue:420]
015          不一致:
016              chPwConfirmError = $t('userChangePasswordNotSame'), return  [PageUser.vue:421]
017      呼叫後端 checkUserPassword 驗證新密碼是否合規  [PageUser.vue:427]
018          state==='success': bCkPw=true 繼續  [PageUser.vue:429]
019          state==='error': chPwNewError = res.msg（語系化策略錯誤訊息）  [PageUser.vue:433]
020          其他/網路錯誤: chPwNewError = $t('userChangePasswordForNetError')  [PageUser.vue:437-442]
021      呼叫後端 changeUserPassword（含 token + lang + 舊密碼 + 新密碼）  [PageUser.vue:450]
022          後端 checkTokenAndChangePassword:
023              checkToken 驗證 token 有效（無效/過期/重複/找不到 → reject）  [procCore.mjs:930]
024              檢查 oldPassword 字串非空  [procCore.mjs:938]
025                  空: reject('invalid oldPassword')  [procCore.mjs:939]
026              getUserByToken 取出使用者 id  [procCore.mjs:943]
027              _getGenUserByKV 以 id 取使用者完整資料（含 password 雜湊）  [procCore.mjs:947]
028              再次呼叫 checkUserPassword（防呆，新密碼策略）  [procCore.mjs:953]
029                  state==='error': reject(對應語系錯誤訊息)  [procCore.mjs:955]
030              比對舊密碼雜湊  [procCore.mjs:962-967]
031                  不符: reject('incorrect old password')  [procCore.mjs:966]
032              檢查使用者 email 非空（用於後續寄通知信）  [procCore.mjs:971]
033                  空: reject('invalid email')  [procCore.mjs:974]
034              新密碼加鹽雜湊後 save 至 users 表（含 isForceChangePw='n', 詳「後台重設使用者密碼」流程交付旗標的回收）  [procCore.mjs:979-985]
035              try: 寄送密碼變更通知信（讀 server/template/changePasswordEmail-{lang}.html）  [procCore.mjs:988-1020]
036              catch: 僅 srLog.error 記錄, 不 reject（變更已成功，寄信失敗不阻擋）  [procCore.mjs:1023-1028]
037      .then 後端成功:
038          bChPw=true  [PageUser.vue:452]
039          cancelChangePassword（收起表單 + 清空 inline errors）  [PageUser.vue:455]
040          $alert($t('userChangePasswordSuccess')) 變更成功通知（保留為 alert，與輸入欄位無關）  [PageUser.vue:457]
041      .catch 後端任何 reject:
042          chPwOldError = $t('userChangePasswordFail')（顯示於舊密碼欄下方紅字，統一訊息不洩露細節）  [PageUser.vue:463]
043  .finally:
044      $ui.updateLoading(false) 隱藏 loading  [PageUser.vue:480]
```

### 四、變更成功後的後續

- 變更成功後使用者繼續處於 user view，**token 不變**（原有 session 保留，不需重新登入）
- 後端 save 時把 `isForceChangePw` 寫為 `'n'`（無論進入流程前是何值），代表變更密碼動作會解除強制變更狀態
- email 通知（若寄信成功）內容由 `server/template/changePasswordEmail-{lang}.html` 控制
- 下次以舊密碼登入會被擋下（密碼已改）；以新密碼登入正常通過
- 其他裝置既有 token 不受影響，仍可使用至 timeEnd（密碼變更**不**強制清除既有 token）

### 五、密碼策略多層檢查設計

策略檢查在前端送出前與後端寫入前各執行一次：

| 階段 | 檢查內容 |
|---|---|
| 前端 UI | 三欄是否填妥 + 兩次密碼一致 |
| 前端 → 後端 `checkUserPassword` API | 策略合規（長度 / 字元類 / 黑名單 / 與帳號連續字元等） |
| 後端 `changeUserPassword` 內部 | 再次呼叫 `checkUserPassword`（防 caller 跳過前端） |
| 後端 比對舊密碼 hash | 不符 reject |
| 後端 寫入 + 寄通知信 | 成功完成 |

兩層 `checkUserPassword`（前端送出前 + 後端 `changeUserPassword` 內）是有意設計：前者給使用者及時 UX 反饋，後者防止 caller 略過前端直接打 API。

## i18n 訊息粒度規則

| 觸發情境 | 對應 key 區位（grep 提示）| 顯示位置 | 粒度規則（spec）|
|---|---|---|---|
| 空欄位（舊密碼）| `server/procLang.mjs` 找 `userChangePasswordForNoOldPassword` | 舊密碼欄下方紅字 | 具體欄位提示 |
| 空欄位（新密碼）| `server/procLang.mjs` 找 `userChangePasswordForNoNewPassword` | 新密碼欄下方紅字 | 具體欄位提示 |
| 空欄位（確認密碼）| `server/procLang.mjs` 找 `userChangePasswordForNoConfirmPassword` | 確認密碼欄下方紅字 | 具體欄位提示 |
| 兩次密碼不一致 | `server/procLang.mjs` 找 `userChangePasswordNotSame` | 確認密碼欄下方紅字 | 直接顯示不一致 |
| 新密碼不符策略（長度 / 字元類 / 黑名單 / 與帳號連續字元等）| `server/procLang.mjs` 找 `userPassword_keyLim*` 系列（與 `checkUserPassword` 回傳 msg key 同源）| 新密碼欄下方紅字 | 顯示**具體策略名**（前端已知策略，逐項列出） |
| 前端呼叫 `checkUserPassword` 網路錯誤 | `server/procLang.mjs` 找 `userChangePasswordForNetError` | 新密碼欄下方紅字 | 統一網路錯誤訊息 |
| 後端任何 reject（舊密碼錯 / token 過期 / email 無效 / 後端 checkUserPassword 防呆 reject）| `server/procLang.mjs` 找 `userChangePasswordFail` | 舊密碼欄下方紅字 | **統一訊息「變更失敗」，不洩露具體 reject 原因** |
| 變更成功通知 | `server/procLang.mjs` 找 `userChangePasswordSuccess` | 全頁 alert | 一般成功通知 |

實際 key 名與翻譯文字 → 寫測試時 grep `server/procLang.mjs` 對應前綴。

## 參數來源

本流程涉及以下執行期參數，**測試讀取順序：`settings.json` 覆寫值 > `server/WWebSso.mjs` opt 預設**：

| 概念 | 來源（優先序）| 測試影響 |
|---|---|---|
| 密碼策略（長度上下限 / 字元類要求 / 禁用字元 / 共用黑名單 / 與帳號連續字元門檻）| `settings.json` `passwordPolicy` → `server/WWebSso.mjs` opt 預設 | 構造「不符策略」case 須避開所有規則才能進入下一階段；構造「合規」case 須繞開帳號連續字元、避開黑名單 |
| 鹽 salt | `settings.json` `salt` → `server/WWebSso.mjs` opt 預設 | 比對 DB password hash 須用同 salt |
| Email 寄信設定（SMTP）| `settings.json` `emSrc*` 系列 | 寄信失敗不影響流程結果，但測試時 SMTP 不通會等網路 timeout（~30s） |

實際數字 / 字元集 → 寫測試時讀 `settings.json` 對應 key，若無 → 讀 `server/WWebSso.mjs` opt 預設值。

## spec 規則摘要（粒度 / 邊界 / 順序 / 契約）

- **驗證順序**：前端空欄三檢查（舊 / 新 / 確認密碼）→ 兩次密碼一致 → 前端呼叫 `checkUserPassword` 策略檢查 → 後端 `checkToken` → 後端 `checkUserPassword`（防呆，防 caller 略過前端）→ 比對舊密碼雜湊 → 檢查 email 非空 → save 新 hash + 寄通知信。任一層 reject 即不寫入。
- **粒度**：
  - 空欄各別具體訊息（指明哪欄空），inline 紅字顯示於對應欄位下方。
  - 兩次密碼不一致 / 策略違反 / 網路錯誤 各自具體訊息。
  - 策略違反帶**具體策略名**（前端已知策略，可逐項列出）。
  - 後端任何 reject（舊密碼錯 / token 過期 / email 無效 / 後端 checkUserPassword 防呆 reject）**統一「變更失敗」訊息**，不洩露 reject 原因。
- **邊界**：
  - 寄信失敗**不阻擋變更結果**（僅 `srLog.error`，使用者體驗為密碼已成功變更）。
  - 「取消」按鈕只清 inline errors，**不清欄位內容**（下次點「變更密碼」展開時才清）。
  - 變更後 `token 不變`（原 session 保留），**其他裝置既有 token 不清除**（trade-off：simplicity）。
- **契約**：
  - 兩層 `checkUserPassword`：前端送出前（UX 反饋） + 後端 `changeUserPassword` 內（防 caller 跳過前端）。
  - save 時 `isForceChangePw='n'` 無條件寫入（解除強制變更狀態，無論進入流程前是何值）。
  - password 加鹽雜湊後寫入 DB（salt 由 settings 提供）。
  - Email 模板路徑：`server/template/changePasswordEmail-{eng,cht}.html`。
