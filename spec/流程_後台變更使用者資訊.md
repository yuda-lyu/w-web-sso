# 後台變更使用者資訊流程

## 觸發

管理員於 LayoutContentUsers.vue 後台 Users list 對既有列就地修改 account / name / email / description / from / redir 等文字欄位，切換 isAdmin / isActive 勾選，調整 timeVerified / timeExpired / timeBlocked 時間欄位，或拖拉列改變順序，再點「儲存」批次提交至後端。

## 重要流程

- **E2E-001**
  - title: 管理員修改既有列欄位儲存成功
  - description: 驗證管理員於後台 Users list 對既有列（target）就地修改 name 欄位後點儲存，後端正確處理 audit 欄位與 password hash 保留之契約：既有 password hash 由後端在 ltdtDiffByKey 前還原回 row 不被前端送回的空字串洗掉；userIdUpdate 與 timeUpdate 由 procOrm save 自動覆寫為操作者 id 與 now；userId 與 timeCreate 保留原值；儲存成功後前端緊接重拉 getUsersList 同步表格。本案例為流程主成功路徑之 baseline 案例，target 使用者於本檔範圍內保留供後續欄位驗證案例使用。
  - flow:
    - 測試資料：admin user `id-mu-admin`（帳號 `mu-admin` / 密碼 `Pw@muadmin1` / `isAdmin='y'` / `isActive='y'`）+ target user `id-mu-target`（帳號 `mu-target` / `name='Modify Target'` / `email='mu-target@test.com'` / `isAdmin='n'` / 預先設定 `timeBlocked='2030-06-01...'` 視為封鎖中 / 持有效未來 60 分鐘 admin token）。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 於 target 列 name cell 雙擊啟動 editor → 改值為 `Modified Target` → 點「Save」/「儲存」按鈕。
    - 驗證（等待 Save 後 success modal 出現）：
      1. 語意：DB target user `name` 已變為 `Modified Target`；`userIdUpdate` 等於 admin.id；`timeUpdate` 非空字串（被後端覆寫為 now）；`password` 欄位仍為原 hash（未被前端空字串洗掉）。
      2. 視覺（多階段，數字前綴使檔名排序即階段順序）：
         - stage1 與 baseline `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-001-1-save-success-modal.png` 視覺一致（pixelmatch 容差）——觀察點為儲存成功 modal（綠勾）畫面，於按確認鈕關閉之前截取。
         - stage2 與 baseline `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-001-2-after-save-modify-name.png` 視覺一致（pixelmatch 容差）——觀察點為關閉 modal、表格重拉後 target 那一列，name 已顯示為 `Modified Target`。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-002**
  - title: account 改為空時前端攔下無法儲存
  - description: 驗證管理員於後台將 target 列 account 欄位改為空字串再點儲存時，前端 computed isError（errItemsByAccount 偵測空值）會聚合出對應錯誤訊息並以 CheckYes modal 顯示，整批不送往後端，DB target 欄位不被儲存。對應 i18n 粒度規則「帳號欄位空」採單一訊息告知。
  - flow:
    - 測試資料：承 E2E-001 之 admin + target user（target 初始 account 為 `mu-target`）。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 於 target 列 account cell 雙擊 → 清空輸入 → 點「Save」/「儲存」按鈕。
    - 驗證（等待 CheckYes modal 出現）：
      1. 語意：modal 文字 eng=`Invalid account of user` / cht=`尚未給予有效使用者帳號`；DB target user `account` 仍為原值 `mu-target`（未被儲存）。
      2. 視覺（分兩階段截圖）：stage1 為按儲存前框 target 列 account cell（已清空、含警示 icon）之觸發狀態 `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-002-1-account-empty-cell.png`；stage2 為 CheckYes 錯誤 modal `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-002-2-account-empty.png`。兩階段均以 pixelmatch 容差比對。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-003**
  - title: account 改為同表內重複時前端攔下無法儲存
  - description: 驗證管理員於後台將 target 列 account 改為與 admin 列相同值再點儲存時，前端 errItemsByAccount 偵測同表重複，isError 聚合出對應訊息並以 CheckYes modal 顯示，整批不送往後端。對應 i18n 粒度規則「帳號同表重複」採不洩露重複對象之單一訊息。
  - flow:
    - 測試資料：承 E2E-001 之 admin（account=`mu-admin`）+ target（account=`mu-target`）user。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 於 target 列 account cell 雙擊 → 改為 `mu-admin`（admin 列 account 值）→ 點「Save」/「儲存」按鈕。
    - 驗證（等待 CheckYes modal 出現）：
      1. 語意：modal 文字 eng=`Duplicate account of user` / cht=`使用者帳號出現重複`；DB target user `account` 仍為原值 `mu-target`。
      2. 視覺（分兩階段截圖）：stage1 為按儲存前框 target 列 account cell（值已改為 admin 列 account → 同表重複）之觸發狀態 `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-003-1-account-duplicate-cell.png`；stage2 為 CheckYes 錯誤 modal `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-003-2-account-duplicate.png`。兩階段均以 pixelmatch 容差比對。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-004**
  - title: email 改為空時前端攔下無法儲存
  - description: 驗證管理員於後台將 target 列 email 欄位改為空字串再點儲存時，前端 errItemsByEmail 偵測空值，isError 聚合出對應訊息並以 CheckYes modal 顯示，整批不送往後端，DB target email 不被儲存。對應 i18n 粒度規則「email 欄位空」採單一訊息告知。
  - flow:
    - 測試資料：承 E2E-001 之 admin + target user（target 初始 email 為 `mu-target@test.com`）。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 於 target 列 email cell 雙擊 → 清空輸入 → 點「Save」/「儲存」按鈕。
    - 驗證（等待 CheckYes modal 出現）：
      1. 語意：modal 文字 eng=`Empty email of user` / cht=`尚未給予使用者Email`；DB target user `email` 仍為原值 `mu-target@test.com`。
      2. 視覺（分兩階段截圖）：stage1 為按儲存前框 target 列 email cell（已清空、含警示 icon）之觸發狀態 `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-004-1-email-empty-cell.png`；stage2 為 CheckYes 錯誤 modal `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-004-2-email-empty.png`。兩階段均以 pixelmatch 容差比對。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-005**
  - title: email 改為格式錯誤時前端攔下無法儲存
  - description: 驗證管理員於後台將 target 列 email 欄位改為不符 isEmail 格式之字串再點儲存時，前端 errItemsByEmail 偵測格式錯，isError 聚合出對應訊息並以 CheckYes modal 顯示，整批不送往後端，DB target email 不被儲存。對應 i18n 粒度規則「email 格式錯」採單一訊息告知。
  - flow:
    - 測試資料：承 E2E-001 之 admin + target user。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 於 target 列 email cell 雙擊 → 改為 `not-an-email-format`（非法格式）→ 點「Save」/「儲存」按鈕。
    - 驗證（等待 CheckYes modal 出現）：
      1. 語意：modal 文字 eng=`Invalid email of user` / cht=`使用者Email格式錯誤`；DB target user `email` 仍為原值 `mu-target@test.com`。
      2. 視覺（分兩階段截圖）：stage1 為按儲存前框 target 列 email cell（值已改為非法格式）之觸發狀態 `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-005-1-email-format-cell.png`；stage2 為 CheckYes 錯誤 modal `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-005-2-email-format.png`。兩階段均以 pixelmatch 容差比對。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-006**
  - title: email 改為同表內重複時前端攔下無法儲存
  - description: 驗證管理員於後台將 target 列 email 改為與 admin 列相同值再點儲存時，前端 errItemsByEmail 偵測同表重複，isError 聚合出對應訊息並以 CheckYes modal 顯示，整批不送往後端。對應 i18n 粒度規則「email 同表重複」採不洩露重複對象之單一訊息。本案例與 E2E-010 共用同一條 i18n 訊息（前後端訊息一致契約）。
  - flow:
    - 測試資料：承 E2E-001 之 admin（email=`mu-admin@test.com`）+ target（email=`mu-target@test.com`）user。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 於 target 列 email cell 雙擊 → 改為 `mu-admin@test.com`（admin 列 email 值）→ 點「Save」/「儲存」按鈕。
    - 驗證（等待 CheckYes modal 出現）：
      1. 語意：modal 文字 eng=`Duplicate email of user` / cht=`使用者Email出現重複`；DB target user `email` 仍為原值 `mu-target@test.com`。
      2. 視覺（分兩階段截圖）：stage1 為按儲存前框 target 列 email cell（值已改為 admin 列 email → 同表重複）之觸發狀態 `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-006-1-email-duplicate-cell.png`；stage2 為 CheckYes 錯誤 modal `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-006-2-email-duplicate.png`。兩階段均以 pixelmatch 容差比對。E2E-010 共用本案例 stage2 modal 之 i18n 訊息（前後端訊息一致契約）。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-007**
  - title: redir 改為空時前端攔下無法儲存
  - description: 驗證管理員於後台將 target 列 redir 欄位改為空字串再點儲存時，前端 errItemsByRedir 偵測空值，isError 聚合出對應訊息並以 CheckYes modal 顯示，整批不送往後端，DB target redir 不被儲存。對應 i18n 粒度規則「redir 欄位空」採單一訊息告知。
  - flow:
    - 測試資料：承 E2E-001 之 admin + target user（target 初始 redir 為 `${baseUrl}/?view=user&token={token}`）。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 於 target 列 redir cell 雙擊 → 清空輸入 → 點「Save」/「儲存」按鈕。
    - 驗證（等待 CheckYes modal 出現）：
      1. 語意：modal 文字 eng=`Invalid redirect of user` / cht=`尚未給予有效登入後轉址`；DB target user `redir` 仍為原值。
      2. 視覺（分兩階段截圖）：stage1 為按儲存前框 target 列 redir cell（已清空、含警示 icon；該欄需水平捲動使其進入視窗）之觸發狀態 `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-007-1-redir-empty-cell.png`；stage2 為 CheckYes 錯誤 modal `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-007-2-redir-empty.png`。兩階段均以 pixelmatch 容差比對。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-008**
  - title: 管理員不可解除自己 admin 權限
  - description: 驗證管理員於後台勾選自己列的 isAdmin checkbox 將其由 'y' 切為 'n' 後點儲存時，前端自我鎖死保護偵測 rows 內操作者本人 isAdmin 已非 'y'，以 CheckYes modal 顯示語系化訊息並不送往後端，admin 自身 isAdmin 不被儲存。後端為第二道防線採同條訊息 reject（本案例驗前端攔截路徑）。
  - flow:
    - 測試資料：承 E2E-001 之 admin（自身 isAdmin='y'）+ target user。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 點 admin 自己列的 isAdmin checkbox（由 'y' 切為 'n'）→ 點「Save」/「儲存」按鈕。
    - 驗證（等待 CheckYes modal 出現）：
      1. 語意：modal 文字 eng=`Cannot demote yourself from admin` / cht=`不可解除自己的管理員權限`；DB admin user `isAdmin` 仍為 `'y'`。
      2. 視覺（分兩階段截圖）：stage1 為按儲存前框 admin 自己列 isAdmin checkbox cell（已取消勾選）之觸發狀態 `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-008-1-isadmin-uncheck-cell.png`；stage2 為 CheckYes 錯誤 modal `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-008-2-cannot-demote-self.png`。兩階段均以 pixelmatch 容差比對。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-009**
  - title: 管理員不可停用自己帳號
  - description: 驗證管理員於後台勾選自己列的 isActive checkbox 將其由 'y' 切為 'n' 後點儲存時，前端自我鎖死保護偵測 rows 內操作者本人 isActive 已非 'y'，以 CheckYes modal 顯示語系化訊息並不送往後端，admin 自身 isActive 不被儲存。後端為第二道防線採同條訊息 reject（本案例驗前端攔截路徑）。
  - flow:
    - 測試資料：承 E2E-001 之 admin（自身 isActive='y'）+ target user。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 點 admin 自己列的 isActive checkbox（由 'y' 切為 'n'）→ 點「Save」/「儲存」按鈕。
    - 驗證（等待 CheckYes modal 出現）：
      1. 語意：modal 文字 eng=`Cannot disable yourself` / cht=`不可停用自己的帳號`；DB admin user `isActive` 仍為 `'y'`。
      2. 視覺（分兩階段截圖）：stage1 為按儲存前框 admin 自己列 isActive checkbox cell（已取消勾選）之觸發狀態 `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-009-1-isactive-uncheck-cell.png`；stage2 為 CheckYes 錯誤 modal `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-009-2-cannot-disable-self.png`。兩階段均以 pixelmatch 容差比對。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-010**
  - title: email 同表重複由前端 errItemsByEmail 攔截顯示 userEmailDuplicate 訊息
  - description: 驗證管理員於後台將 target 列 email 改為與 admin 列相同值再點儲存時，前端 errItemsByEmail 偵測同表重複並以 CheckYes modal 顯示 userEmailDuplicate 訊息，整批不送往後端。後端 ckKey('email') 作為防衛第二道，但其 reject 為原始技術字串而非 i18n key，正常 UI 流程下不會被觸及。本案例操作與 E2E-006 相同，僅作為確認「前端攔截是主要防線」之補充紀錄；驗證 E2E-006 共用同一 modal 訊息即可。
  - flow:
    - 測試資料：承 E2E-001 之 admin（email=`mu-admin@test.com`）+ target（email=`mu-target@test.com`）user。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 於 target 列 email cell 雙擊 → 改為 `mu-admin@test.com` → 點「Save」/「儲存」按鈕。
    - 驗證（等待 CheckYes modal 出現）：
      1. 語意：modal 文字 eng=`Duplicate email of user` / cht=`使用者Email出現重複`（與 E2E-006 同訊息）；DB target user `email` 仍為原值 `mu-target@test.com`。
      2. 視覺：與 baseline `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-010-email-conflict-backend.png` 視覺一致（pixelmatch 容差）。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-011**
  - title: 儲存時 token 已過期後端 reject
  - description: 驗證管理員修改欄位後於點儲存的時點 token 已過期之場景：後端 checkToken 偵測 token 失效後 reject，前端以 CheckYes modal 顯示 `userSaveUsersFail` 前綴之語系化訊息（不洩露身分檢查與真過期差異），整批不落地，DB 目標欄位不被儲存。本案例透過後端直接將 admin token 的 timeEnd 設為過去模擬「修改完之後 token 才過期」的時序，屬 spec「token 已過期 / 已被刪後端 reject」契約。
  - flow:
    - 測試資料：承 E2E-001 之 admin（持有效 token）+ target user（name=`Modify Target`）。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 於 target 列 name cell 雙擊 → 改為 `Modified With Expired Token` → 後端強制將 admin token timeEnd 設為過去（模擬點儲存時 token 已過期）→ 點「Save」/「儲存」按鈕。
    - 驗證（等待 CheckYes modal 出現）：
      1. 語意：modal 文字格式為 `${userSaveUsersFail 翻譯}: ${tokenExpired 翻譯}`；eng 呈現 `Failed to save users: Your session has expired. Please log in again.` / cht 呈現 `儲存使用者數據失敗: 登入逾時，請重新登入。`；DB target user `name` 仍為原值 `Modify Target`。
      2. 視覺：與 baseline `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-011-token-expired-backend.png` 視覺一致（pixelmatch 容差）。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-012**
  - title: 修改 timeVerified 以 timemsTZ 寫回並同步至 DB
  - description: 驗證管理員於後台透過 WTimeminute 日期選擇器修改 target 列的 timeVerified 欄位，前端以 timemsTZ 格式寫回 row 並標記 isModified，儲存時走 diff 群組由 procOrm save 同步至 DB；timeExpired 與 timeBlocked 不被本案例動到。本案例驗單一時間欄位之 timemsTZ 寫入契約與其他時間欄位互不干擾。
  - flow:
    - 測試資料：承 E2E-001 之 admin + target user（target 預設 timeVerified=`2025-01-01T00:00:00.000+08:00` / timeExpired=`2030-01-01T00:00:00.000+08:00` / timeBlocked=`2030-06-01T00:00:00.000+08:00`）。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 於 target 列 timeVerified cell 點開 WTimeminute 日期選擇 popup → 點「15」日選項 → 關閉 popup → 點「Save」/「儲存」按鈕。
    - 驗證（等待 Save 後 success modal 出現）：
      1. 語意：DB target user `timeVerified` 已變更（非原 `2025-01-01...` 也非空）且為 timemsTZ 格式；`timeExpired` 仍為 `2030-01-01T00:00:00.000+08:00`、`timeBlocked` 仍為 `2030-06-01T00:00:00.000+08:00`（未被本案例動到）。
      2. 視覺（多階段，數字前綴使檔名排序即階段順序）：
         - stage1 與 baseline `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-012-1-save-success-modal.png` 視覺一致（pixelmatch 容差）——觀察點為儲存成功 modal（綠勾）畫面，於按確認鈕關閉之前截取。
         - stage2 與 baseline `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-012-2-time-modify-verified.png` 視覺一致（pixelmatch 容差）——觀察點為關閉 modal、表格重拉後 target 那一列，timeVerified 欄位顯示改後的日期。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-013**
  - title: 修改 timeExpired 以 timemsTZ 寫回並同步至 DB
  - description: 驗證管理員於後台透過 WTimeminute 日期選擇器修改 target 列的 timeExpired 欄位，前端以 timemsTZ 格式寫回 row 並標記 isModified，儲存時走 diff 群組由 procOrm save 同步至 DB；timeVerified 與 timeBlocked 不被本案例動到。
  - flow:
    - 測試資料：承 E2E-001 之 admin + target user（target 預設 timeVerified=`2025-01-01T00:00:00.000+08:00` / timeExpired=`2030-01-01T00:00:00.000+08:00` / timeBlocked=`2030-06-01T00:00:00.000+08:00`）。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 於 target 列 timeExpired cell 點開 WTimeminute 日期選擇 popup → 點「15」日選項 → 關閉 popup → 點「Save」/「儲存」按鈕。
    - 驗證（等待 Save 後 success modal 出現）：
      1. 語意：DB target user `timeExpired` 已變更（非原 `2030-01-01...` 也非空）且為 timemsTZ 格式；`timeVerified` 仍為 `2025-01-01T00:00:00.000+08:00`、`timeBlocked` 仍為 `2030-06-01T00:00:00.000+08:00`（未被本案例動到）。
      2. 視覺（多階段，數字前綴使檔名排序即階段順序）：
         - stage1 與 baseline `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-013-1-save-success-modal.png` 視覺一致（pixelmatch 容差）——觀察點為儲存成功 modal（綠勾）畫面，於按確認鈕關閉之前截取。
         - stage2 與 baseline `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-013-2-time-modify-expired.png` 視覺一致（pixelmatch 容差）——觀察點為關閉 modal、表格重拉後 target 那一列，timeExpired 欄位顯示改後的日期。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-014**
  - title: 修改 timeBlocked 以 timemsTZ 寫回並同步至 DB
  - description: 驗證管理員於後台透過 WTimeminute 日期選擇器修改 target 列的 timeBlocked 欄位，前端以 timemsTZ 格式寫回 row 並標記 isModified，儲存時走 diff 群組由 procOrm save 同步至 DB；timeVerified 與 timeExpired 不被本案例動到。本案例 seed 將 target.timeBlocked 預設為未來時間（將 target 視為封鎖中），避開 WTimeminute popup 在 value 為空時的邊界行為，符合 spec「修改 timeBlocked」之契約。
  - flow:
    - 測試資料：承 E2E-001 之 admin + target user（target.timeBlocked 預設 `2030-06-01T00:00:00.000+08:00` 視為封鎖中以避開空值邊界）。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 於 target 列 timeBlocked cell 點開 WTimeminute 日期選擇 popup → 點「15」日選項 → 關閉 popup → 點「Save」/「儲存」按鈕。
    - 驗證（等待 Save 後 success modal 出現）：
      1. 語意：DB target user `timeBlocked` 已變更（非原 `2030-06-01T00:00:00.000+08:00`）且為 timemsTZ 格式；`timeVerified` 仍為 `2025-01-01T00:00:00.000+08:00`、`timeExpired` 仍為 `2030-01-01T00:00:00.000+08:00`（未被本案例動到）。
      2. 視覺（多階段，數字前綴使檔名排序即階段順序）：
         - stage1 與 baseline `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-014-1-save-success-modal.png` 視覺一致（pixelmatch 容差）——觀察點為儲存成功 modal（綠勾）畫面，於按確認鈕關閉之前截取。
         - stage2 與 baseline `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-014-2-time-modify-blocked.png` 視覺一致（pixelmatch 容差）——觀察點為關閉 modal、表格重拉後 target 那一列，timeBlocked 欄位顯示改後的日期。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

- **E2E-015**
  - title: 拖拉列調整順序後端重指派 order
  - description: 驗證管理員於後台拖拉 target 列至 admin 列之前以調整顯示順序，儲存時後端 updateTabItems 以 resetOrder=true 依新陣列順序重指派 order=k+1，前端送回的 order 值被覆寫；一次拖拉可能讓多列同時進入 diff 群組。本案例驗 spec「拖拉列順序變動 → 後端 resetOrder=true 重指派」之契約，以 DB 中 target.order 嚴格小於 admin.order 斷言新順序生效。
  - flow:
    - 測試資料：承 E2E-001 之 admin（初始 order=890）+ target（初始 order=891）user。
    - 操作：以 admin token 進入後台 Users list 並切到編輯模式 → 於 target 列 account 欄左側 drag handle 按下並拖至 admin 列上緣 → 放開（target 移到 admin 之前）→ 點「Save」/「儲存」按鈕。
    - 驗證（等待 Save 後 success modal 出現）：
      1. 語意：DB target user `order` 嚴格小於 DB admin user `order`（後端 resetOrder 依新陣列順序重指派 k+1）。
      2. 視覺：與 baseline `test/pics/modifyuser/modifyuser-{eng,cht}-E2E-015-drag-reorder.png` 視覺一致（pixelmatch 容差）。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 admin/target user 與 admin token，保留 base seed。

## 執行流程

### 一、就地編輯欄位（資料表 cell-render）

```
account / email / redir / name / description / from
    aggrid 內建 cell editor 直接輸入文字 (kpCellEditable 定義);
    cell 顯示警告 icon 表示有 errItemsBy* 偵測到錯誤;
    輸入完成 → rowsChange → vo.isModified=true

password
    顯示為「重設密碼」按鈕, 不接受就地輸入

isAdmin / isActive
    checkbox 形式; click 走 $dg.toggleItemIsAdminById / toggleItemIsActiveById
        → toggleItemByKeyAndId(key, id) 切 'y' / 'n', set 至 opt.rows[kr].<key>
        → vo.refresh() 觸發 cell 重渲染, vo.isModified=true

timeVerified / timeExpired / timeBlocked
    左側狀態指示器顯示 isVerified / isExpired / isBlocked (綠勾 / 紅叉) 反映目前 row 狀態;
    右側 WTimeminute 日期時間選擇器 @input → handleCellTimeInput(key, id, timeNew)
        → 解析後以 timemsTZ 格式寫回 row 對應欄位
        → vo.refresh(), vo.isModified=true

順序
    account 欄啟用 kpRowDrag.account=true, 可拖拉列調整 opt.rows 順序;
    觸發 rowsChange → vo.isModified=true
```

### 二、即時前端驗證（computed）

前端 computed 對每列做粒度驗證，產生 cell 警告 icon + tooltip，並聚合為 `isError` 字串供 saveUsers 攔截。粒度規則詳「i18n 訊息粒度規則」章節。

```
errItemsByAccount   每列檢查 account 空值與同表重複, 寫入 kpErr[<value>] = 對應錯誤訊息
errItemsByEmail     每列檢查 email 空值 / isEmail 格式 / 同表重複
errItemsByRedir     每列檢查 redir 空值

isError computed（整體錯誤訊息, 第一層攔截）
    依序取 errItemsByAccount → errItemsByPassword → errItemsByEmail → errItemsByRedir
    第一條具體錯誤訊息 (cell tooltip 級訊息, 非 category title);
    cellFieldErr 在 cell 顯示警告 icon + tooltip 細節錯誤
```

### 三、儲存（saveUsers）

```
001  管理員點紅底「儲存」按鈕（僅 isEditable && isModified 顯示）
002  $ui.updateLoading(true)
003  檢查 isError
004      非空字串:
005          hideLoading + await showCheckYes(isError) 後 return（不送後端）
006  rows = vo.opt.rows
007  檢查 rows 是否為空 → 為空 hideLoading + await showCheckYes(對應訊息) return
008  自我鎖死保護（前端第一道）:
009      尋找 rows 內 row.id === userSelf.id
009a     若操作者 id 非空且 selfRow 為 null（自我刪除）→ hideLoading + await showCheckYes(cannotDeleteSelf) return
010      若該列 isAdmin !== 'y' → hideLoading + await showCheckYes(cannotDemoteSelf) return
011      若該列 isActive !== 'y' → hideLoading + await showCheckYes(cannotDisableSelf) return
012  剝除 transient flag _isNew (不送後端)
013  vo.$fapi.updateUsersList(token, lang, rows) 批次提交
014      handler 帶入 funCheckAdmin 後呼叫 p.checkTokenAndUpdateUsersList(token, lang, rows, opt)
015          checkToken({ fun: funCheckAdmin })
016              token 為空 / 過期 / 無效 → reject
017              token 有效但 funCheckAdmin 回 false（非 admin）→ b2=false → reject
018                  （注意: 此路徑不分歧錯誤訊息, 與真過期共用同一字串）
019          getUserByToken 取操作者 → operatorId
020          自我鎖死保護（後端第二道）:
020a             rows 內找不到操作者 row（自我刪除）→ reject(get(kpLang, lang.cannotDeleteSelf) 已翻譯文字)
021              rows 內找操作者 row, 若 isAdmin !== 'y' → reject(get(kpLang, lang.cannotDemoteSelf) 已翻譯文字)
022                                     若 isActive !== 'y' → reject(get(kpLang, lang.cannotDisableSelf) 已翻譯文字)
             （後端 reject 為已翻譯文字而非 key；前端 $tErr 查無對應 key 時直接顯示此文字，使用者體驗上與前端訊息相同）
023          updateUsersList(rows, { lang, operatorId }) 委派
024              updateTabItems('users', rows, 'id', { resetOrder: true, lang, operatorId })
025                  ltdtmapping(rows, ds.users.keys) 補齊缺失欄位
026                  resetOrder: rows 依陣列順序重指派 order = k+1
027                  ckKey('id'): 表內 id 不可為空或重複 → reject
028                  ckKey('email'): 表內 email 不可為空或重複 → reject
029                  ltdtOld = woItems.users.select() 取 DB 既有資料
029a                 account 唯一性 + 非空驗證 (補 id/email 之外的 account, 防直打 API 寫入空/重複帳號; 以 ltdtOld 建立既有 account→id 對照, 排除本批同 id 之自身既有列後逐列檢查)  [procCore.mjs:1419-1451]
029b                     空值: reject('accountRequired')  [procCore.mjs:1435]
029c                     本批內重複 或 與既有其他 user 衝突: reject('accountDuplicate')  [procCore.mjs:1440,1400]
030                  既有 row password 保留: 將 ltdtOld 內對應 id 的 password (hash) 寫回 rows 該列
031                      （getUsersList 已 strip password, 前端送回的 password 是 '', 此步避免覆蓋既有 hash）
032                  ltdtDiffByKey(ltdtOld, rows, 'id') 偵測有差異的列, 收進 diff 群組
033                  diff 群組（既有 row 有變動）:
034                      procOrm(operatorId, 'users', 'save', r.diff)
035                          procOrm 內部對 save 群組自動覆寫:
036                              userIdUpdate = operatorId
037                              timeUpdate = now
038                          （userId / timeCreate 不被覆寫, 保留原始建立資訊）
039                          （password 已在步驟 030 還原為 DB hash, save 時 merge 不會覆蓋）
040                  return ltdtNew (剝除 password 欄位避免 hash 經 API 洩漏)
041  .catch err → errTemp = err
042  errTemp 非 null:
043      msg = isestr(errTemp) ? $tErr(errTemp) : $t('anUnexpectedErrorOccurred')
         hideLoading + await $dg.showCheckYes(`${$t('userSaveUsersFail')}: ${msg}`) 後 return
            （errTemp 為後端 reject 之 i18n key 字串, 經 $tErr 翻譯後拼入 userSaveUsersFail 前綴；
              改用 modal 強制 admin 點擊確認, 避免 transient toast 漏看後端錯誤訊息）
044  $fapi.getUsersList(token) 重拉同步, 覆寫 vo.users
045      （讓 userIdUpdate=operatorId、timeUpdate=now、order 重排等後端寫入結果立即顯示）
046  isModified = false（紅底儲存按鈕收起）
047  hideLoading + await showCheckYes(<userSaveUsersSuccess>)
```

### 四、衍生操作（同一表共用 saveUsers 路徑）

```
toggleItemIsAdminById / toggleItemIsActiveById
    checkbox click → 切 'y'/'n'，isModified=true
    儲存時走 diff 群組（procOrm save）;
    若被切的列 id === userSelf.id 且新值非 'y', 儲存階段會被自我鎖死保護擋下

handleCellTimeInput (timeVerified / timeExpired / timeBlocked)
    WTimeminute @input → 以 timemsTZ 格式寫回 row 對應欄位, isModified=true
    儲存時走 diff 群組（procOrm save）

rowDrag (透過 kpRowDrag.account=true 啟用)
    拖拉列 → opt.rows 陣列順序變動 → rowsChange → isModified=true
    儲存時 updateTabItems({ resetOrder: true }) 由後端依新陣列順序重指派 order=k+1
```

### 五、可變更／不可變更欄位邊界（純 spec）

| 欄位 | 可否就地變更 | 邊界規則 / 後端處理 |
|---|---|---|
| account | 可（cell editor）| 不可空、全域不可重複（前端 errItemsByAccount 攔下同表空值/重複；後端 `updateTabItems` 另補必填 + 全域唯一性驗證，reject `accountRequired` / `accountDuplicate`，防直打 API 寫入空/重複帳號）|
| name | 可（cell editor）| 無限制 |
| email | 可（cell editor）| 不可空、需 isEmail 格式、表內不重複；後端 `ckKey('email')` 全表再檢查 |
| description / from | 可（cell editor）| 無限制 |
| redir | 可（cell editor）| 不可空 |
| isAdmin | 可（checkbox）| 自我鎖死保護：不可將自己改為非 'y'（前後端兩道）|
| isActive | 可（checkbox）| 自我鎖死保護：不可將自己改為非 'y'（前後端兩道）|
| timeVerified / timeExpired / timeBlocked | 可（WTimeminute）| timemsTZ 格式寫回 row，procOrm save merge 寫入 |
| order | 可（拖拉）| 後端 resetOrder=true 依新陣列順序重指派 order=k+1；前端送回的 order 值會被覆寫 |
| password | 不接受就地輸入（顯示為「重設密碼」按鈕；既有列密碼欄位點按鈕可觸發重設流程）| 後端 saveUsers 在 ltdtDiffByKey 前以 ltdtOld 對應 id 的 hash 還原 row.password，避免前端送回空字串把 DB hash 洗掉 |
| id | 不可（kpHeadHide，偵測鍵）| ckKey('id') 確認不重複 |
| userId / timeCreate | 不可（前端不渲染為輸入元件）| procOrm save 不覆寫此兩欄位，保留原始建立資訊 |
| userIdUpdate / timeUpdate | 不可（前端不渲染為輸入元件）| procOrm save 自動覆寫為操作者 id 與 now |
| isForceChangePw / tokenVerify | 不在 tabKeysShow / tabKeysPick 內 | — |

## i18n 訊息粒度規則

| 觸發情境 | 對應 key 區位（grep 提示）| 顯示位置 | 粒度規則（spec）|
|---|---|---|---|
| 帳號欄位空 | `server/procLang.mjs` 找 `userAccountEmpty` | cell 警告 icon + tooltip | 單一訊息告知帳號空 |
| 帳號同表重複 | `server/procLang.mjs` 找 `userAccountDuplicate` | cell 警告 icon + tooltip | 不洩露重複對象 |
| email 欄位空 | `server/procLang.mjs` 找 `userEmailEmpty` | cell 警告 icon + tooltip | 單一訊息告知 email 空 |
| email 格式錯 | `server/procLang.mjs` 找 `userEmailError` | cell 警告 icon + tooltip | 單一訊息告知 email 格式不符 |
| email 同表重複 | `server/procLang.mjs` 找 `userEmailDuplicate` | cell 警告 icon + tooltip | 不洩露重複對象 |
| redir 欄位空 | `server/procLang.mjs` 找 `userRedirEmpty` | cell 警告 icon + tooltip | 單一訊息告知 redir 空 |
| 解除自己 admin 權限 | `server/procLang.mjs` 找 `cannotDemoteSelf` | CheckYes modal（前端先擋）+ 後端 reject（同訊息）| 前後端統一 |
| 停用自己帳號 | `server/procLang.mjs` 找 `cannotDisableSelf` | CheckYes modal（前端先擋）+ 後端 reject（同訊息）| 前後端統一 |
| 後端 token 過期 / 非 admin | `server/procLang.mjs` 找 `tokenExpired`（checkToken 所有分支統一 reject 此 key；funCheckAdmin 判否亦收斂至同 key）| CheckYes modal，前綴 `userSaveUsersFail`，錯誤文字由 `$tErr('tokenExpired')` 翻譯 | 不分歧：身分檢查失敗與真過期共用，不洩露差別 |
| 後端 ckKey('email') 重複 | `server/procCore.mjs` ckKey 內部 reject 原始技術字串（非 procLang key）；在正常 UI 流程下前端 errItemsByEmail 先行攔截，後端 ckKey 作為防衛第二道，不洩漏至使用者 | CheckYes modal，前綴 `userSaveUsersFail`，錯誤文字為原始技術字串 | 前端攔截訊息與 userEmailDuplicate 相同；若繞過前端直打 API 後端訊息為原始技術字串 |
| 後端 account 必填 / 全域唯一性防衛 | `server/procLang.mjs` 找 `accountRequired` / `accountDuplicate`（合法 procLang key，非原始技術字串）；在正常 UI 流程下前端 errItemsByAccount 先行攔截，後端此驗證作為防衛第二道 | CheckYes modal，前綴 `userSaveUsersFail`，錯誤文字由 `$tErr` 翻譯 | 前端已攔截空值 / 同表重複；後端另補「與既有 DB user 衝突」之全域唯一性檢查，文字與前端 `userAccountEmpty` / `userAccountDuplicate` 不同 |
| 儲存成功 | `server/procLang.mjs` 找 `userSaveUsersSuccess` | CheckYes modal | — |
| 儲存失敗（包裝層）| `server/procLang.mjs` 找 `userSaveUsersFail` | CheckYes modal | 作為後端 reject 訊息的前綴 |

實際 key 名與翻譯文字 → 寫測試時 grep `server/procLang.mjs` 對應前綴。

## 參數來源

本流程涉及以下執行期參數，**測試讀取順序：settings.json 覆寫值 > WWebSso.mjs opt 預設**：

| 概念 | 來源（優先序）| 測試影響 |
|---|---|---|
| 使用者欄位 schema（哪些欄位存在、欄位預設值、kpHeadHide 隱藏項等）| `src/schema/users.mjs` | test setup 建 user 與 modify 後 DB 斷言基準 |
| salt（hashPassword）| `settings.json` 對應 key → 預設值 | 比對「password hash 未被洗掉」時須使用同 salt |
| token 有效期 | `settings.json` 對應 key | 測試 token 構造時的 `timeEnd` 計算 |

實際數字 / 預設值 → 寫測試時讀 `settings.json` 對應 key 或 `src/schema/users.mjs` 的 funNew。

## spec 規則摘要（粒度 / 邊界 / 順序 / 契約）

- **驗證順序**：前端 computed `errItemsBy*` → 前端 `isError` 聚合攔截 → 前端自我鎖死保護 → 後端 `checkToken`（含 funCheckAdmin）→ 後端 `checkTokenAndUpdateUsersList` 自我鎖死保護 → `ckKey('id')` / `ckKey('email')` → account 必填 + 全域唯一性驗證（reject `accountRequired` / `accountDuplicate`）→ procOrm save 寫入。任一層 reject 即整批不落地。
- **粒度**：account / email 「空」與「重複」分開告知；token 失效與身分不足共用同一訊息（不洩露身分檢查）；前後端攔截訊息一致。
- **邊界**：
  - password 不可由本流程修改（顯示為「重設密碼」按鈕，不接受就地輸入）。後端 saveUsers 在 ltdtDiffByKey 前以 DB 既有 hash 還原 row.password，避免前端送回空字串把 DB hash 洗掉。
  - 自我鎖死保護：rows 內若包含操作者本人，不可將自己 `isAdmin` 或 `isActive` 改為非 'y'（前端 + 後端兩道）。
  - userId / timeCreate 保留原值，不被覆寫；userIdUpdate / timeUpdate 由 procOrm save 自動覆寫為操作者 id 與 now（audit 不接受前端送來的值）。
- **契約**：
  - `updateUsersList` 走批次 upsert：整表 `ltdtDiffByKey` 一次同步 add / diff / del，任一驗證錯誤即整批不落地。即使 admin 只改一列，ckKey 仍對全表掃描。
  - 拖拉列順序變動 → 後端 `resetOrder=true` 依陣列順序重指派 order=k+1，前端送回的 order 值被覆寫；一次拖拉可能讓多列同時進入 diff 群組。
  - 儲存成功後立即重拉 `getUsersList` 同步 audit fields 與 order。
  - API 回傳前剝除 password 欄位，避免 hash 從 API 流出。
  - token 不因 user 資料更新而失效（token 表獨立，不級聯）。
