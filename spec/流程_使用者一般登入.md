# 使用者一般登入流程

## 觸發

使用者在 PageLogin.vue 輸入帳號密碼，點擊「Log in」按鈕。

## 重要流程

> 編號規則:
> - `**E2E-NNN**`: UI baseline 測試, 對應 `test/e2e-login.test.mjs` + `test/pics/login/login-{lang}-E2E-NNN-*.png`
> - `**E2E-NNN(it-only)**`: 有 it() 案例但無 baseline (元件級互動測試)

- **E2E-001** 使用者輸入帳密成功登入
- **E2E-002** 使用者輸入帳密失敗, 密碼錯 (E2E-002-wrong-pw)
- **E2E-003** 使用者輸入帳密但未驗證, 顯示重寄 UI (E2E-003-notverify-login-failed)
- **E2E-004** 使用者點重寄 UI, 重寄表單展開 (E2E-004-notverify-resend-page)
- **E2E-005** 使用者填表單送出重寄, 重寄成功 (E2E-005-notverify-resend-sent)
- **E2E-006** 使用者收信點驗證連結, 已驗證結果頁 (E2E-006-notverify-verified)
- **E2E-007** 已驗證後再次登入即成功 (E2E-007-notverify-logged-in)
- **E2E-008** 使用者輸入帳密但帳號已被停用 (isActive !== 'y')
- **E2E-009** 使用者輸入帳密但帳號已過期 (timeExpired 已過)
- **E2E-010** 使用者輸入帳密但帳號被暫時封鎖 (背景 timer 自動封鎖判定後寫入 timeBlocked)
- **E2E-011** 登入成功 URL view=backstage, 停留 backstage (E2E-011-view-backstage)
- **E2E-012** 登入成功 URL view=user, 停留 user view (E2E-012-view-user)
- **E2E-013** 重寄輸入不存在帳號, 統一訊息不洩露 (013-resend-invalid-account, 屬未驗證重寄細項)
- **E2E-014** 重寄輸入已驗證帳號, 拒絕重寄 (014-resend-already-verified, 屬未驗證重寄細項)
- **E2E-015** 使用者輸入帳密失敗, 帳號不存在 (015-account-not-exist 共用 002 baseline, 防帳號列舉)
- **E2E-016** 登入成功但 user.redir 為空, alert 並回登入頁 (E2E-016-login-no-redir)
- **E2E-017** 使用者於登入頁的密碼欄, 預設顯示為遮罩 (input type=password, 右側 eye icon = mdi-eye-off) (E2E-017-password-hidden; eng 一份, 無 cht 因純 UI 元件)
- **E2E-018** 使用者於登入頁的密碼欄, 點擊 eye icon 切換為明文 (input type=text, icon = mdi-eye); 再點擊回遮罩視覺等同 E2E-017, 故僅 DOM 驗證不再 baseline (E2E-018-password-shown)

## 執行流程

```
001  從 URL 取得目標頁面 view ('login'|'backstage'|'user'，無參數預設 'login')  [PageLogin.vue:568]
002  呼叫 mUI.login，帶入帳號、密碼、是否轉址 (view==='login' 時啟用轉址)  [PageLogin.vue:571]
003      執行非同步流程 core()  [mUI.mjs:340]
004          呼叫後端 loginByAccountAndPassword，無 catch，reject 時 throw 至 .catch  [mUI.mjs:364]
005              呼叫保護層 pp.loginByAccountAndPassword (非直接 procCore)  [WWebSso.mjs:1140]
006                  檢查帳號是否被封鎖 (比對 timeBlocked 與當前時間)  [procProtect.mjs:153]
007                      封鎖中:
008                          reject('account blocked')  [procProtect.mjs:158]
009                  呼叫核心層 p.loginByAccountAndPassword  [procProtect.mjs:163]
010                      以帳號查詢使用者 (不限 isActive，以便逐一檢查狀態)  [procCore.mjs:248]
011                          找不到:
012                              reject('incorrect user account or password')  [procCore.mjs:250]
013                      檢查帳號是否啟用 (isActive)  [procCore.mjs:255]
014                          非 'y':
015                              reject('account inactive')  [procCore.mjs:257]
016                      比對密碼雜湊  [procCore.mjs:265]
017                          不一致:
018                              reject('incorrect user account or password')  [procCore.mjs:266]
019                      檢查 email 是否已驗證 (timeVerified 是否有值)  [procCore.mjs:270]
020                          未驗證:
021                              reject('account not verified')  [procCore.mjs:272]
022                      檢查帳號是否過期 (timeExpired 是否已過)  [procCore.mjs:276]
023                          已過期:
024                              reject('account expired')  [procCore.mjs:280]
025                      建立登入 token (到期時間 = 現在 + minExpired 分鐘)  [procCore.mjs:289]
026                      回傳使用者資訊與 token  [procCore.mjs:293]
027                  .then  [procProtect.mjs:164]
028                      清除該帳號的登入失敗紀錄  [procProtect.mjs:174]
029                  .catch  [procProtect.mjs:177]
030                      記錄本次失敗時間  [procProtect.mjs:186]
031                  檢查結果  [procProtect.mjs:198]
032                      成功(state==='success'):
033                          回傳使用者物件  [procProtect.mjs:199]
034                      失敗(state==='error'):
035                          reject 錯誤訊息  [procProtect.mjs:201]
036              .then  [WWebSso.mjs:1141]
037                  暫存使用者物件  [WWebSso.mjs:1142]
038              .catch  [WWebSso.mjs:1144]
039                  暫存錯誤訊息  [WWebSso.mjs:1145]
040              檢查使用者物件  [WWebSso.mjs:1149]
041                  有值:
042                      回傳使用者物件  [WWebSso.mjs:1157]
043                  無值:
044                      reject 錯誤訊息  [WWebSso.mjs:1159]
045          檢查回傳的使用者物件與 token  [mUI.mjs:375]
046              無效:
047                  reject，中止  [mUI.mjs:379]
048          更新前端狀態 (token + 使用者資訊)，將 token 存入 localStorage  [mUI.mjs:401]
049          檢查是否啟用轉址  [mUI.mjs:408]
050              useRedir=true 且有 redir:
051                  轉址至 redir 網址，結束  [mUI.mjs:427]
052              useRedir=true 但無 redir:
053                  顯示無有效轉址錯誤，中止  [mUI.mjs:418]
054              useRedir=false:
055                  登入完成，resolve  [mUI.mjs:443]
056      .catch  [mUI.mjs:449]
057          上拋原始 err 給 PageLogin (pm.reject)  [mUI.mjs:456]
058  .then  [PageLogin.vue:572]
059      前往 view 指定的功能頁  [PageLogin.vue:575]
060  .catch(err)  [PageLogin.vue:579]
061      檢查錯誤類型，依錯誤字串選擇對應 i18n 訊息顯示  [PageLogin.vue:582]
062      返回登入頁  [PageLogin.vue:604]
```

## i18n 訊息粒度規則

後端依序檢查 (1) 帳號封鎖 → (2) 帳號存在 → (3) 帳號啟用 → (4) 密碼正確 → (5) email 已驗證 → (6) 帳號未過期 → (7) token 建立。檢查順序屬 spec，不得隨意調動 (重排會洩露帳號存在性)。

前端依後端 reject 字串對映至 i18n 鍵後顯示對應語系翻譯，**錯誤字串本身不直接顯示給使用者**。

| 觸發情境 | i18n 鍵區位 (grep 提示) | 顯示位置 | 粒度規則 (spec) |
|---|---|---|---|
| 帳號不存在 | `server/procLang.mjs` 找 `failedLoginForCatch` 等通用登入失敗鍵 | PageLogin 錯誤訊息區 | **與「密碼錯誤」共用同一條訊息，禁止洩露帳號是否存在 (防帳號列舉)** |
| 密碼錯誤 | 同上 | 同上 | 同上 |
| 帳號已停用 (isActive !== 'y') | 同上 | 同上 | **與「帳號不存在」共用同一條訊息**：procProtect 階段以 `can not find the user by account` reject，前端視同帳號不存在 (防帳號列舉) |
| 帳號被封鎖 (timeBlocked > now) | `server/procLang.mjs` 找 `loginAccountBlocked` | PageLogin 錯誤訊息區 | 顯示帳號暫時鎖定的固定訊息，**不洩露失敗次數 / 解鎖時間** |
| 帳號未驗證 (無 timeVerified) | `server/procLang.mjs` 找 `userRegistrationNotVerified` 等註冊未驗證系列鍵 | PageLogin 錯誤訊息區 + 重寄驗證信連結 | 顯示尚未驗證的固定訊息，並提供重寄驗證信入口 |
| 帳號已過期 (timeExpired < now) | `server/procLang.mjs` 找 `loginAccountExpired` | PageLogin 錯誤訊息區 | 顯示帳號已過期的固定訊息 |
| 重寄驗證信時 email 不符 | `server/procLang.mjs` 找重寄驗證信錯誤系列鍵 (`userRegistration*` 前綴) | PageLogin 重寄驗證信錯誤訊息區 | 訊息可明確指出 email 不符 (此時使用者已通過帳密前置條件，不再有列舉風險) |
| 重寄驗證信但已驗證 | 同上 | 同上 | 訊息可明確指出已驗證 |
| 登入成功但 redir 為空 (view=login 場景) | `server/procLang.mjs` 找 `failedLoginForNoRedir` | PageLogin 錯誤訊息區 | 顯示無有效轉址的固定訊息 |
| 未預期錯誤 (catch 內無對應分支) | `server/procLang.mjs` 找 `failedLoginForCatch` | PageLogin 錯誤訊息區 | console.log 原始 err 供除錯，UI 顯示與「帳密錯誤」相同的通用訊息 |

實際 i18n 鍵名與翻譯文字 → 寫測試時 grep `server/procLang.mjs` 對應前綴後直接讀取，避免在本文件 mirror 翻譯字串造成 drift。

## 登入成功分流

| URL 條件 | useRedir | redir 欄位 | 結果 |
|---|---|---|---|
| 無 view 或 `view=login` | true | 有值 | 轉址至 redir 網址 |
| 無 view 或 `view=login` | true | 無值 | 顯示無有效轉址錯誤 (見 i18n 訊息粒度規則表) |
| `view=backstage` | false | 不看 | 留在前端進入後台 |
| `view=user` | false | 不看 | 留在前端進入使用者資訊頁 |

## 參數來源

本流程涉及以下執行期參數，**讀取優先序：`settings.json` 覆寫值 > `server/WWebSso.mjs` 預設**：

| 概念 | 來源 (優先序) | 流程影響 |
|---|---|---|
| Token 有效期 (建立 token 時 timeEnd = now + N 分鐘) | `settings.json` 找 token 有效期相關 key → `server/WWebSso.mjs` opt 預設 | 影響登入後 token 多久過期、自動登入流程可用窗口 |
| 失敗次數 / 觀察分鐘數 / 封鎖時長 (背景 timer 判定 timeBlocked 用) | `settings.json` 找登入失敗相關 key → `server/WWebSso.mjs` opt 預設 | 影響「帳號被暫時封鎖」觸發條件，本流程僅讀取 timeBlocked 結果，不直接消費這些閾值 |

實際數值 → 寫測試時讀 `settings.json` 對應 key，若無覆寫則查 `server/WWebSso.mjs` opt 預設。

## spec 規則摘要（粒度 / 邊界 / 順序 / 契約）

- **驗證順序**：protect 層 `timeBlocked` 檢查 → 核心查 user → `isActive` 檢查 → 密碼比對 → `timeVerified` 檢查 → `timeExpired` 檢查 → 建立 token。**順序屬 spec 不得調動**（調動會洩露帳號存在性）。
- **粒度**：
  - 帳號不存在 / 密碼錯誤 / `isActive='n'` **共用同一條訊息**，禁止洩露帳號是否存在（防帳號列舉）。
  - 帳號封鎖（顯示固定「暫時鎖定」訊息）/ 未驗證（顯示重寄連結）/ 已過期 各自具體訊息。
  - 重寄驗證信時 email 不符可洩露（使用者已通過帳密前置條件，無列舉風險）。
- **邊界**：
  - 核心層查 user **不限 `isActive`**（以便逐一檢查狀態，再由 spec 順序決定何時 reject）。
  - 登入成功分流以 URL `view` 為準（`login` / `backstage` / `user`）。
  - `view=login` 須有 `redir`，無 `redir` 顯示無有效轉址錯誤。
- **契約**：
  - protect 層 reject 字串原樣上拋給 `mUI.login`，再原樣上拋給 `PageLogin`；PageLogin 依錯誤字串對應 i18n 鍵後顯示，**錯誤字串本身不直接顯示**。
  - 登入成功須同時取得 user object 與 token，任一無效 reject。
  - Token 建立時 `timeEnd = now + minExpired 分鐘`；本流程結束後 token 寫入 localStorage 供自動登入使用。
