# 後台統計資訊流程

## 觸發

管理員登入後台後，左側導覽預設停在「Statistics information」（後台統計資訊頁，`LayoutContentStaInfor.vue`）。此頁聚合呈現使用者 / 金鑰 / IP 三大面向之計數卡片、頻率圖表（登入 / 金鑰 / IP）與管控狀態卡片，提供管理員一頁綜覽系統運行狀況。

後端 `getStaUserSummary` / `getStaTokenSummary` / `getStaIpSummary` / `getStaUserAccountLogin` / `getStaToken` / `getStaIp` 共 6 個 RPC 皆以 `funCheckAdmin` 包裝（`server/WWebSso.mjs:1276,1260,1269,1278,1287,1296`；`funCheckAdmin` 定義於 `server/WWebSso.mjs:551`），**僅具 admin 權限的有效 token 可存取**；token 無效／過期／非 admin 一律 reject。

## 重要流程

- **E2E-001**
  - title: 管理員進入統計資訊頁顯示初始檢視態
  - description: 驗證管理員以有效 admin token 登入後台後進入「Statistics information」頁，前端 mounted 階段呼叫 `getAndRelaData` 並行呼叫 6 個 `getSta*` API，後端各自回傳統計結果，前端依序綁定至使用者區（total/active/locked/expired）、管控區（active/created24h/ending24h/ended/conn24h/blocked）卡片與三個頻率圖表（freqLogin/freqToken/freqIp）。本案例為此流程之入口檢視，後續失敗案例皆以此為對照基準。屬正常成功路徑案例。
  - flow:
    - 測試資料：admin user（帳號 `stainfor-admin` / 密碼 `Pw@stainfor1` / `isActive='y'` / `isAdmin='y'` / `timeVerified` 有值 / `redir` 帶 `?view=backstage&token={token}`）+ 有效 admin token（timeEnd 為 60 分鐘後）+ base seed（3 users + 4 tokens + 0 ips）。
    - 操作：開乾淨登入頁（localStorage 無 token）→ 輸入帳號與密碼 → 點「Log in」/「登入」按鈕 → 跳 backstage 預設停在 Statistics 頁 → 等所有 `getSta*` API 回完。
    - 驗證：
      1. 語意（DOM 文字）：頁面含「Statistics information」/「統計資訊」標題 + 使用者資訊區「Total Users」/「總使用者」標籤 + 管控狀態區「Active tokens」/「有效金鑰數量」標籤。
      2. 視覺：與 baseline `test/pics/stainfor/stainfor-{eng,cht}-E2E-001-page-loaded.png` 視覺一致（pixelmatch 容差）。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 user 與 admin token，保留 base seed。

- **E2E-002**
  - title: 管理員 token 失效時頁面顯示載入失敗訊息
  - description: 驗證管理員以有效 admin token 登入後台進入 Statistics 頁後，若 token 過期失效，再次回到 Statistics 頁時（元件 unmount 後 remount，重新觸發 mounted hook），`getAndRelaData` 以 `Promise.allSettled` 並行 6 個 `getSta*` API，因 token 驗證失敗全數 reject；`Promise.allSettled` 等全部 settle 後 rejected 任一即 throw，mounted 外層 `.catch` 設 `errMsg=$t('getDataError')`。template `v-if="!errMsg"` 為 false → 主內容（卡片 + 圖表 + 表格）隱藏，進入 `v-else` 之 `v-if="errMsg"` 顯示 `getDataError` 訊息（eng「Failed to get data, please try again later」/ cht「取得數據失敗，請稍後再試」）。屬非正常路徑案例，與 E2E-001 形成「有效 token 顯示主內容 / 失效 token 顯示錯誤訊息」之對照。
  - flow:
    - 測試資料：admin user（帳號 `stainfor-admin` / 密碼 `Pw@stainfor1` / `isActive='y'` / `isAdmin='y'` / `timeVerified` 有值 / `redir` 帶 `?view=backstage&token={token}`）+ admin token（初始 timeEnd 為 60 分鐘後，將於進入 Statistics 第二輪前由後端強制改為 1 分鐘前過期）+ base seed（3 users + 4 tokens + 0 ips）。
    - 操作：開乾淨登入頁（localStorage 無 token）→ 輸入帳號與密碼 → 點「Log in」/「登入」按鈕 → 跳 backstage 預設停在 Statistics 頁 → 切到「Users」/「使用者清單」（觸發 Statistics 元件 unmount）→ 後端強制將該 admin token 之 `timeEnd` 改為 1 分鐘前 → 點「Statistics information」/「統計資訊」回到 Statistics 頁（元件 remount，mounted hook 重新觸發 `getAndRelaData`，`Promise.allSettled` 並行 6 個 `getSta*` 全因 token 過期 reject → throw 至 mounted 外層 `.catch` 設 `errMsg`）→ 等錯誤訊息渲染完成。
    - 驗證：
      1. 語意（DOM 文字）：頁面顯示 `getDataError` 對應文字（eng「Failed to get data, please try again later」/ cht「取得數據失敗，請稍後再試」），且主內容區域之卡片標籤（如「Total Users」/「總使用者」）不再出現於 DOM（主內容被 `v-if="!errMsg"` 隱藏）。
      2. 視覺：與 baseline `test/pics/stainfor/stainfor-{eng,cht}-E2E-002-admin-token-expired-page-empty.png` 視覺一致（pixelmatch 容差）（主代理將重產，預期僅見 page header + 錯誤訊息文字，無卡片區塊）。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 user 與 admin token，保留 base seed。

- **E2E-003**
  - title: 統計頁顯示「使用者登入頻率」圖表
  - description: 驗證管理員進入 Statistics 頁後,「存取活動監測」區之「使用者登入頻率」直條圖正常呈現(近 7 天嘗試/成功/失敗登入次數)。屬展示功能之獨立佐證案例。spec 粒度規則: 此圖為動態圖表 canvas, 跨進程渲染漂移、且 X 軸為 wall-clock「近 7 天」會位移, 故視覺 baseline 以「預存真實快照貼圖覆蓋」穩定化(baseline 與比對兩端貼同一快照, 呈現真實圖表而非遮黑); 語意層讀 live DOM 驗結構, 不驗會飄的計數值。
  - flow:
    - 測試資料：admin user(同 E2E-001) + 有效 admin token + base seed; 前置「系統已累積使用者登入活動紀錄(嘗試/成功/失敗)」使圖表有長條。
    - 操作：登入 admin → 進 Statistics 頁 → 等資料載入 → 捲動使「使用者登入頻率」圖置中於視窗(完整呈現、不被視窗邊界裁切)。
    - 驗證：
      1. 語意(DOM 文字)：含「User Login Frequency」/「使用者登入頻率」標題, 且該圖區塊內有圖表 canvas 呈現。
      2. 視覺：與 baseline `test/pics/stainfor/stainfor-{eng,cht}-E2E-003-user-login-frequency-chart.png` 視覺一致(pixelmatch 容差); 標注紅框為該圖本身; 視覺以預存快照貼圖覆蓋穩定化(見 description 粒度規則)。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 user 與 admin token，保留 base seed。

- **E2E-004**
  - title: 統計頁顯示「金鑰使用頻率」圖表
  - description: 驗證 Statistics 頁「金鑰使用頻率」直條圖正常呈現(近 7 天各金鑰驗證次數)。屬展示功能佐證。spec 粒度規則同 E2E-003(canvas + wall-clock → 預存快照貼圖覆蓋穩定化, 語意驗結構不驗計數)。本案例與 E2E-005(金鑰用量表)同屬金鑰區塊但各自獨立聚焦(本案只框圖、E2E-005 只框表)。
  - flow:
    - 測試資料：admin user(同 E2E-001) + 有效 admin token + base seed; 前置「系統已有金鑰驗證活動紀錄(各金鑰被驗證)」使圖表有長條、用量表有列。
    - 操作：登入 admin → 進 Statistics 頁 → 等資料載入 → 捲動使「金鑰使用頻率」圖置中於視窗(完整不裁切)。
    - 驗證：
      1. 語意(DOM 文字)：含「Token Usage Frequency」/「金鑰使用頻率」標題, 且該圖區塊內有圖表 canvas 呈現。
      2. 視覺：與 baseline `test/pics/stainfor/stainfor-{eng,cht}-E2E-004-token-usage-frequency-chart.png` 視覺一致(pixelmatch 容差); 紅框為該圖本身; 預存快照貼圖覆蓋穩定化。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 user 與 admin token，保留 base seed。

- **E2E-005**
  - title: 統計頁顯示「金鑰對應使用者用量統計」表
  - description: 驗證 Statistics 頁金鑰區塊內之「金鑰對應使用者用量統計」表格正常呈現(每位使用者之金鑰使用總計與近 1 天/8 時/4 時/1 時計數)。此表 `v-if` 於有金鑰使用量時才渲染。屬展示功能佐證。spec 粒度規則: 表內「近 N 時」為 wall-clock 時間窗計數會飄, 故視覺以預存快照貼圖覆蓋穩定化; 語意驗表頭與「有資料列」結構, 不驗會飄的計數值。
  - flow:
    - 測試資料：admin user(同 E2E-001) + 有效 admin token + base seed; 前置「系統已有金鑰驗證活動紀錄」使表格有資料列(否則 v-if 不渲染)。
    - 操作：登入 admin → 進 Statistics 頁 → 等資料載入 → 捲動使「金鑰對應使用者用量統計」表置中於視窗(完整不裁切)。
    - 驗證：
      1. 語意(DOM 文字)：含表頭(排序/使用者姓名/總計/近1天/近8時/近4時/近1時, eng 對應 numberOrder/username/total/last1Day/last8Hour/last4Hour/last1Hour 之 i18n), 且表身至少一資料列存在。
      2. 視覺：與 baseline `test/pics/stainfor/stainfor-{eng,cht}-E2E-005-token-user-usage-table.png` 視覺一致(pixelmatch 容差); 紅框為該表本身; 預存快照貼圖覆蓋穩定化。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 user 與 admin token，保留 base seed。

- **E2E-006**
  - title: 統計頁顯示「IP 連線頻率」圖表
  - description: 驗證 Statistics 頁「IP 連線頻率」直條圖正常呈現(近 7 天各 IP 連線次數)。屬展示功能佐證。spec 粒度規則同 E2E-003(canvas + wall-clock → 預存快照貼圖覆蓋穩定化, 語意驗結構)。與 E2E-007(IP 用量表)同屬 IP 區塊但各自獨立聚焦(本案只框圖)。
  - flow:
    - 測試資料：admin user(同 E2E-001) + 有效 admin token + base seed; 前置「系統已有來自多個 IP 的連線活動紀錄」使圖表有長條、用量表有列。
    - 操作：登入 admin → 進 Statistics 頁 → 等資料載入 → 捲動使「IP 連線頻率」圖置中於視窗(完整不裁切)。
    - 驗證：
      1. 語意(DOM 文字)：含「IP Connection Frequency」/「IP 連線頻率」標題, 且該圖區塊內有圖表 canvas 呈現。
      2. 視覺：與 baseline `test/pics/stainfor/stainfor-{eng,cht}-E2E-006-ip-connection-frequency-chart.png` 視覺一致(pixelmatch 容差); 紅框為該圖本身; 預存快照貼圖覆蓋穩定化。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 user 與 admin token，保留 base seed。

- **E2E-007**
  - title: 統計頁顯示「IP 使用量統計」表
  - description: 驗證 Statistics 頁 IP 區塊內之「IP 使用量統計」表格正常呈現(每個 IP 之連線總計與近 1 天/8 時/4 時/1 時計數)。此表 `v-if` 於有 IP 使用量時才渲染。屬展示功能佐證。spec 粒度規則同 E2E-005(近 N 時為 wall-clock 計數會飄 → 預存快照貼圖覆蓋穩定化; 語意驗表頭與有資料列)。
  - flow:
    - 測試資料：admin user(同 E2E-001) + 有效 admin token + base seed; 加上**固定 fixture log**（每案於前置寫入 log 目錄一個非 ISO 檔名之檔案，內含 4 個固定 IP `203.0.113.11~14` 之連線紀錄，時間相對執行當下推算為 2 小時前與 26 小時前、皆落於 7 天窗內；本機 e2e 之真實連線 IP 只有 `127.0.0.1`，後端刻意不讀 `x-forwarded-for`），使表恆有 ≥5 個 IP → 預設顯示 5 列、表格高度確定不隨歷史資料漂移；近 N 時計數仍為 wall-clock 會飄，由預存快照貼圖覆蓋處理。fixture 於每案清理時移除。
    - 操作：登入 admin → 進 Statistics 頁 → 等資料載入 → 捲動使「IP 使用量統計」表置中於視窗(完整不裁切)。
    - 驗證：
      1. 語意(DOM 文字)：含表頭(排序/IP/總計/近1天/近8時/近4時/近1時), 且表身至少一資料列存在。
      2. 視覺：與 baseline `test/pics/stainfor/stainfor-{eng,cht}-E2E-007-ip-usage-table.png` 視覺一致(pixelmatch 容差); 紅框為該表本身; 預存快照貼圖覆蓋穩定化。
    - 雙語：eng / cht 各一輪。
    - 清理：移除本檔特化 user 與 admin token，保留 base seed。

## 執行流程

```
001  使用者開 Statistics 頁, 觸發 mounted hook  [LayoutContentStaInfor.vue:407]
002      設 errMsg=''  [LayoutContentStaInfor.vue:413]
003      template 立即渲染主內容骨架 (頁首 + 各區塊標題); 各區塊資料未到前以 WIconLoading 轉圈佔位 (先到先畫, 無整頁等待文字)  [LayoutContentStaInfor.vue:11,39,111,146,218]
004      呼叫 getAndRelaData()  [LayoutContentStaInfor.vue:416]
005          Promise.allSettled 並行 6 個 getSta* API  [LayoutContentStaInfor.vue:769-776]
006              grStaUserSummary()  [LayoutContentStaInfor.vue:636]
007                  loadingUserSummary = true (卡片數字改顯示轉圈)  [LayoutContentStaInfor.vue:643]
008                  fapi.getStaUserSummary(token)  [LayoutContentStaInfor.vue:644]
009                      checkToken(token, {fun: funCheckAdmin})  [procStaInfor.mjs]
010                          token 無效 / 過期 / 非 admin:
011                              reject  → 內部 .catch
012                      回傳 {total, active, locked, expired}
013                  .then 設 userOverview = res  [LayoutContentStaInfor.vue:645-647]
014                  .catch console.log + throw err 向上 propagate  [LayoutContentStaInfor.vue:648-651]
015                  .finally loadingUserSummary = false (該區塊即刻改顯示數字, 不等其他 API)  [LayoutContentStaInfor.vue:652-654]
016              grStaTokenSummary() / grStaIpSummary(): 同款 loadingTokenSummary / loadingIpSummary 旗標 + .finally  [LayoutContentStaInfor.vue:665-675, 687-697]
017              grStaUserAccountLogin() / grStaToken() / grStaIp(): .then 綁定 freqLogin / freqToken / freqIp 並產生 optLogin / optToken / optIp; 圖表區以 optX 是否為 null 切換轉圈與圖  [LayoutContentStaInfor.vue:104-114]
018                  各自 .catch console.log + throw err 向上 propagate
019          Promise.allSettled 等全部 settle (不短路)  [LayoutContentStaInfor.vue:769]
020          檢查 rejected 任一:
021              有 rejected:
022                  console.log 每個 rejected.reason  [LayoutContentStaInfor.vue:781]
023                  throw rejected[0].reason  → mounted 外層 .catch  [LayoutContentStaInfor.vue:782]
024              全 fulfilled:
025                  正常結束
026      mounted .catch (接住 getAndRelaData throw):
027          errMsg = $t('getDataError')  [LayoutContentStaInfor.vue:417-420]
028      template render  [LayoutContentStaInfor.vue:11,307]
029          v-if="!errMsg" → 主內容 (卡片 + 圖表 + 表格; 各區塊載入中為轉圈)
030          v-else (= errMsg 有值):
031              v-if="errMsg" → 顯示 errMsg (任一 getSta* reject 時觸發)
```

## i18n 訊息粒度規則

| 元素類別 | i18n 鍵 (grep `server/procLang.mjs`) | 顯示元件 | 規則 |
|---|---|---|---|
| 頁面主標題 | `statisticsInformation` | `<div>` 標題 | 純顯示 |
| 頁面副標題 | `statisticsInformationDescription` | `<div>` 副標題 | 純顯示 |
| 使用者區標籤 | `userGroupInfor` / `totalUsers` / `activeUsers` / `blockedUsers` / `expiredUsers` | 卡片 label | 純顯示 |
| 圖表區標題 | `accessActivityMonitoring` / `userLoginFrequency` / `tokenUsageFrequency` / `ipConnectionFrequency` | 圖表 title | 純顯示 |
| 管控區標籤 | `controlStatus` / `tokenCharacteristic` / `activeTokens` / `createdLast24h` / `endingNext24h` / `endedTokens` / `ipCharacteristic` / `conn24h` / `blockedIps` | 卡片 label | 純顯示 |
| 等待文字 | `waitingData` | 三張頻率圖之 300px 佔位區（`WIconLoading` 轉圈 + 文字） | 該圖資料未到前顯示；卡片與管控狀態數值改以轉圈佔位不帶文字；無整頁等待文字（各區塊先到先畫，ADR-051 P2） |
| 錯誤文字 | `getDataError` | `<div>` 文字 | 載入失敗顯示，任一 `getSta*` reject 時觸發（`getAndRelaData` 之 `Promise.allSettled` 收集後 throw → mounted 外層 `.catch` 設 `errMsg`，template `v-else` 內 `v-if="errMsg"` 顯示） |

實際翻譯文字 → 寫測試時 grep `server/procLang.mjs` 對應鍵後直接讀取。

## 參數來源

| 概念 | 來源 | 流程影響 |
|---|---|---|
| 操作者 token | 前端 store（登入後 admin token） | 後端以 `funCheckAdmin` 驗證；失效則所有 `getSta*` reject，前端 reactive 變數維持初始空值 |
| 統計時間範圍 | 後端 `procStaInfor` 內部固定（最近 7 天 + 各時間粒度 1hr/4hr/8hr/1day） | 與請求時間相對；本檔之 seed `timeCreate` 設為 2025-01-01（>1 年前），三個頻率圖表均落在 7 天視窗外 → 圖表渲染空資料 |

## spec 規則摘要

- **權限**：6 個 `getSta*` 皆 admin-only（`funCheckAdmin`）。
- **粒度**：本頁為純顯示（無修改 / 刪除 / 儲存）。不會觸發 `showCheckYes` modal 或 `$alert` toast。
- **邊界**：頁面交互元素（時間粒度選擇 / 個別顯示 checkbox / 顯示全部連結）僅影響前端圖表渲染樣式，不會重新呼叫後端 API；故本流程不涵蓋此類 UI 互動之 e2e。
- **契約**：mounted 時 `getAndRelaData` 用 `Promise.allSettled` 並行 6 個 `getSta*`，任一 reject 即向上 propagate 至 mounted 外層 `.catch` 設 `errMsg=$t('getDataError')`；6 個 `grStaXxx` 內部 `.catch` 各自 `console.log` 後 `throw err` 讓錯誤可向上 propagate。template `v-if="!errMsg"` 為 true 即顯示主內容骨架（卡片 + 圖表 + 表格），各區塊各自於資料到達時渲染（卡片與管控狀態以 `loadingUserSummary` / `loadingTokenSummary` / `loadingIpSummary` 旗標切換轉圈與數字，三張圖以 `optX` 是否為 null 切換），先到先畫，不等最慢者；`errMsg` 有值才進入 `v-else` 顯示錯誤文字。載入完成後之 DOM 與改造前一致（轉圈僅存在於載入中），既有 baseline 不受影響。錯誤路徑對齊 `LayoutContentUserInfor` canonical pattern；先到先畫為 ADR-051 P2。
- **截圖範圍取捨**：E2E-001 的 視覺一致（pixelmatch 容差） baseline 僅截上方 cards 區（y:0..330），刻意排除下方三個頻率圖表。理由：圖表呈現「最近 7 天」資料且依登入活動 bucket 化（1hr 預設粒度），spike 位置會隨測試執行的小時 bucket 變動，跨時間 視覺一致（pixelmatch 容差） 不穩；而 cards 區（Total Users / Active Users / Blocked Users / Expired Users）僅依賴 DB seed 計數（與 wall-clock 無關），可確定性穩定。下方三個頻率圖表與兩個用量表(展示功能)之呈現另由 **E2E-003~007** 各自獨立涵蓋(每項捲到置中、紅框聚焦單一圖/表)：因圖表為動態 canvas(跨進程 GPU 渲染漂移)、且圖表 X 軸與表格「近 N 時」皆為 wall-clock 時間窗會位移, 無法直接 pixel 守護, 故該五案視覺以「預存真實快照貼圖覆蓋」穩定化(兩端貼同一快照, 呈現真實圖/表而非遮黑), 語意層讀 live DOM 驗結構(標題/表頭/有資料列), 不驗會飄的計數值。E2E-001 仍維持僅截 cards 區(確定性穩定)之取捨。E2E-002 之 `errMsg` 顯示路徑亦適用同一 clip（y:0..330）：錯誤訊息 div 位於 `v-else` 內、`padding:10px 15px`、`font-size:0.8rem`，渲染於 page header 之下（約 y=60-100），落在 0..330 範圍內可完整涵蓋；同時主內容被 `v-if="!errMsg"` 隱藏，clip 範圍內亦無圖表干擾。
