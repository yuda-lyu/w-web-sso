# 規格書 7：密碼過期通知機制

> 依賴：規格書 1（passwordPolicy）、規格書 3（使用者表結構擴充）、規格書 6（登入時密碼狀態檢查）
> 被依賴：無（最上層延伸功能）


## 一、目的

建立排程機制，定期檢查所有使用者的密碼是否即將過期或已過期：
- **即將過期**：提前通知使用者主動變更密碼
- **已過期**：自動標記為「待變更密碼」，產生 resetToken，寄送重設連結

此機制確保密碼過期不僅是「登入時被動阻擋」，而是「主動通知使用者處理」。


## 二、前提條件

- `passwordPolicy.expireDays > 0`（若為 0 表示永不過期，不啟動此機制）
- Email 服務可正常運作


## 三、排程設計

### 3.1 settings.json 新增設定

```javascript
passwordPolicy: {
    // ... 既有欄位 ...
    expireDays: 90,                  // 密碼過期天數
    expireNotifyDays: [30, 7, 1],    // 到期前 N 天寄送提醒通知
    expireCheckIntervalHours: 24,    // 排程檢查間隔（小時）
},
```

| 欄位 | 類型 | 說明 |
|------|------|------|
| `expireNotifyDays` | Array\<Integer\> | 到期前幾天寄送提醒，可設定多個時間點 |
| `expireCheckIntervalHours` | Integer | 排程執行間隔，預設 24 小時 |

### 3.2 啟動檢查（WWebSso.mjs）

```javascript
// 伺服器啟動時檢查
if (!isearr(passwordPolicy.expireNotifyDays)) {
    throw new Error('invalid passwordPolicy.expireNotifyDays')
}
let expireCheckIntervalHours = get(passwordPolicy, 'expireCheckIntervalHours')
if (!ispint(expireCheckIntervalHours) || cint(expireCheckIntervalHours) < 1) {
    throw new Error('invalid passwordPolicy.expireCheckIntervalHours')
}
```


## 四、排程邏輯

### 4.1 位置

在 `WWebSso.mjs` 中，於伺服器啟動完成後啟動排程：

```javascript
// 啟動密碼過期檢查排程
if (passwordPolicy.expireDays > 0) {
    let intervalMs = passwordPolicy.expireCheckIntervalHours * 60 * 60 * 1000

    // 啟動時立即執行一次
    p.checkPasswordExpiry()

    // 定期執行
    setInterval(() => {
        p.checkPasswordExpiry()
    }, intervalMs)
}
```

### 4.2 procCore.mjs — 新增 checkPasswordExpiry

**函式簽名：**
```javascript
let checkPasswordExpiry = async () => { ... }
```

**邏輯：**
```
1. 若 passwordPolicy.expireDays === 0 → 直接 return

2. 取得所有 active 使用者：
   let users = await woItems.users.select({ isActive: 'y' })

3. 逐一檢查每位使用者：
   for each user:

     // 跳過已在待變更狀態的使用者（避免重複處理）
     if (user.isPendingPasswordReset === 'y') continue

     // 取得密碼變更時間
     let timeChanged = user.timePasswordChanged || user.timeCreate
     if (!istimemsTZ(timeChanged)) continue

     // 計算過期時間
     let expireTime = ot(timeChanged).add(passwordPolicy.expireDays, 'day')
     let now = ot()
     let daysRemaining = expireTime.diff(now, 'day')

     if (daysRemaining < 0):
         // === 已過期：自動觸發重設流程 ===
         a. 產生 resetToken
         b. 設定 isPendingPasswordReset = 'y'
         c. 設定 passwordResetToken、timePasswordResetExpired
         d. 清除該使用者所有登入 token
         e. 寄送「密碼已過期，請重設」Email
         f. srLog.info({ event: 'password-expired-auto-reset', userId })

     else if (expireNotifyDays.includes(daysRemaining)):
         // === 即將過期：寄送提醒 ===
         a. 寄送「密碼即將過期」提醒 Email
         b. srLog.info({ event: 'password-expiry-notify', userId, daysRemaining })
```

### 4.3 防止重複通知

需避免同一天多次執行排程導致重複寄信。有兩種方案：

**方案 A（簡單）：** 排程間隔設為 24 小時，且 `expireNotifyDays` 以天為單位，自然不會重複。

**方案 B（嚴謹）：** 在使用者表新增 `lastExpireNotifyDate` 欄位，記錄最後一次發送過期通知的日期，檢查時比對避免重複。

建議先採用**方案 A**，保持簡單。


## 五、Email 模板

### settings.json 新增

```javascript
expirePwEmTitle: {
    eng: 'Password expiration reminder',
    cht: '密碼即將到期提醒',
},
expirePwEmContent: {
    eng: 'Dear User {name},<br><br>Your password for {sender} will expire in {days} day(s).<br><br>Please change your password before it expires to avoid losing access to your account.<br><br>This is an automated system notification. Please do not reply to this email.',
    cht: '使用者 {name} 您好：<br><br>您於「{sender}」的密碼將於 {days} 天後到期。<br><br>請於到期前變更密碼，以避免無法登入。<br><br>此為系統自動通知信件，請勿直接回覆。',
},
expiredPwEmTitle: {
    eng: 'Password has expired',
    cht: '密碼已過期通知',
},
expiredPwEmContent: {
    eng: 'Dear User {name},<br><br>Your password for {sender} has expired. Your account has been locked until a new password is set.<br><br>Please click the link below to set a new password. This link will expire in {hours} hours:<br><br><a href="{resetUrl}">{resetUrl}</a><br><br>If you have any questions, please contact the system administrator.<br><br>This is an automated system notification. Please do not reply to this email.',
    cht: '使用者 {name} 您好：<br><br>您於「{sender}」的密碼已過期，帳號已被鎖定直至設定新密碼。<br><br>請點擊以下連結設定新密碼，此連結將於 {hours} 小時後失效：<br><br><a href="{resetUrl}">{resetUrl}</a><br><br>如有任何問題，請聯絡系統管理單位。<br><br>此為系統自動通知信件，請勿直接回覆。',
},
```

### WWebSso.mjs 讀取

```javascript
let expirePwEmTitle = get(opt, 'expirePwEmTitle', '')
let expirePwEmContent = get(opt, 'expirePwEmContent', '')
let expiredPwEmTitle = get(opt, 'expiredPwEmTitle', '')
let expiredPwEmContent = get(opt, 'expiredPwEmContent', '')
```


## 六、語系

### procLang.mjs 新增

```javascript
timePasswordChanged: {
    eng: 'Password changed time',
    cht: '最後密碼變更時間',
},
passwordExpireDays: {
    eng: 'Password expires in {days} day(s)',
    cht: '密碼將於 {days} 天後到期',
},
```


## 七、與其他規格書的協作

| 情境 | 觸發者 | 動作 |
|------|--------|------|
| 管理員重設密碼 | 規格書 4 | 標記待變更、寄重設連結 |
| 使用者完成變更 | 規格書 5 | 更新 timePasswordChanged、清除標記 |
| 使用者登入時密碼已過期 | 規格書 6 | 拒絕登入、提示聯絡管理員 |
| 排程偵測即將過期 | 本規格書 | 寄送提醒 Email |
| 排程偵測已過期 | 本規格書 | 自動標記待變更、寄重設連結（等同管理員重設） |
| 使用者自行變更密碼 | 現有 PageUser.vue | 需同步更新 timePasswordChanged |


## 八、現有 changeUserPassword 調整

使用者自行變更密碼（`checkTokenAndChangePassword`，procCore.mjs:691）成功後，需同步更新 `timePasswordChanged`：

```javascript
// 現有 save 區塊增加 timePasswordChanged
await woItems.users.save({
    id: userId,
    password: passwordNew,
    timePasswordChanged: ot().format('YYYY-MM-DDTHH:mm:ss.SSSZ'),  // 新增
})
```


## 九、影響範圍

| 檔案 | 變動 |
|------|------|
| `settings.json` | passwordPolicy 新增 expireNotifyDays、expireCheckIntervalHours；新增 Email 模板 |
| `server/WWebSso.mjs` | 啟動檢查新增欄位驗證、啟動排程、讀取 Email 模板 |
| `server/procCore.mjs` | 新增 `checkPasswordExpiry`、調整 `checkTokenAndChangePassword` |
| `server/procLang.mjs` | 新增語系鍵 |


## 十、注意事項

1. **Email 失敗容錯：** 排程寄信失敗不應中斷整個排程，需 try-catch 逐筆處理
2. **效能：** 使用者數量大時，排程需注意效能。可考慮分批處理或加入索引
3. **時區：** 所有時間計算需統一時區，沿用現有 timemsTZ 格式（含時區資訊）
4. **伺服器重啟：** 排程使用 setInterval，伺服器重啟時會自動重新啟動，無狀態問題
