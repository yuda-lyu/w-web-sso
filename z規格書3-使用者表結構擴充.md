# 規格書 3：使用者表結構擴充

> 依賴：無（獨立資料結構變更）
> 被依賴：規格書 4、5、6、7


## 一、目的

在 users 表新增欄位，支援：
1. 管理員重設密碼後標記帳號為「待變更密碼」
2. 記錄密碼最後變更時間，供密碼過期機制使用
3. 儲存密碼重設 token，供使用者透過信件連結變更密碼


## 二、新增欄位

### users.mjs schema 新增

| 欄位 | 類型 | 說明 | 預設值 |
|------|------|------|--------|
| `isPendingPasswordReset` | STRING | 是否待變更密碼，'y' 或 'n' | 'n' |
| `timePasswordChanged` | STRING | 最後密碼變更時間（timemsTZ 格式） | 與 timeCreate 相同 |
| `passwordResetToken` | STRING | 密碼重設用 token | '' |
| `timePasswordResetExpired` | STRING | 重設 token 過期時間（timemsTZ 格式） | '' |

### schema 定義

```javascript
// 在 users.mjs 的 settings 物件中新增：

isPendingPasswordReset: {
    name: '是否待變更密碼',
    type: 'STRING',
},
timePasswordChanged: {
    name: '最後密碼變更時間',
    type: 'STRING',
},
passwordResetToken: {
    name: '密碼重設用token',
    type: 'STRING',
},
timePasswordResetExpired: {
    name: '密碼重設token過期時間',
    type: 'STRING',
},
```

### funNew 調整

```javascript
let funNew = (ndata = {}) => {
    let o = dtmapping(ndata, keys(settings))
    o.id = `${genIDSeq()}`
    o.timeCreate = nowms2str()
    o.userIdUpdate = o.userId
    o.timeUpdate = o.timeCreate
    o.isActive = 'y'
    o.isPendingPasswordReset = 'n'          // 新增
    o.timePasswordChanged = o.timeCreate    // 新增，初始密碼變更時間等同創建時間
    o.passwordResetToken = ''               // 新增
    o.timePasswordResetExpired = ''         // 新增
    return o
}
```


## 三、各欄位用途對應

| 欄位 | 寫入時機 | 讀取時機 |
|------|----------|----------|
| `isPendingPasswordReset` | 管理員重設密碼時設為 'y'；使用者完成密碼變更後設為 'n' | 登入時檢查，若 'y' 則拒絕登入 |
| `timePasswordChanged` | 使用者變更密碼、管理員重設密碼時更新 | 密碼過期檢查時比對 |
| `passwordResetToken` | 管理員重設密碼時產生並儲存 | 使用者透過連結變更密碼時驗證 |
| `timePasswordResetExpired` | 管理員重設密碼時設定（當前時間 + resetExpireHours） | 使用者透過連結變更密碼時檢查是否過期 |


## 四、既有資料相容性

### LMDB 資料庫特性

LMDB（w-orm-lmdb）為 NoSQL 型態，不需要 migration。新欄位在舊資料中自動為 `undefined`。

### 相容處理

需在讀取時對舊資料做防禦處理：

```javascript
// 讀取時，若欄位不存在則視為預設值
let isPending = get(user, 'isPendingPasswordReset', 'n')
let timePasswordChanged = get(user, 'timePasswordChanged', '')
```

這些防禦處理分散在各使用處（登入檢查、密碼過期檢查等），不需集中處理。


## 五、安全性

### 敏感欄位不回傳前端

現有 procCore.mjs 中 `_getGenUserByKV` 已有 `deletePassword` 機制刪除 password 欄位。需同步將以下欄位也從回傳前端的資料中移除：

- `passwordResetToken` — 絕不可回傳前端
- `timePasswordResetExpired` — 不需回傳前端

### getUsersList 調整

管理員查看 Users list 時，`isPendingPasswordReset` 與 `timePasswordChanged` 可顯示在前端（供管理員了解狀態），但 `passwordResetToken` 必須移除。


## 六、前端欄位顯示

### LayoutContentUsers.vue

可選擇是否在 aggrid 中新增顯示欄位：

| 欄位 | 是否顯示 | 顯示方式 |
|------|----------|----------|
| `isPendingPasswordReset` | 可選 | 勾選框或狀態文字 |
| `timePasswordChanged` | 可選 | 時間格式 |
| `passwordResetToken` | 否 | — |
| `timePasswordResetExpired` | 否 | — |

此部分可於規格書 4（管理員重設密碼）中一併處理。


## 七、影響範圍

| 檔案 | 變動 |
|------|------|
| `src/schema/tables/users.mjs` | 新增 4 個欄位定義、調整 funNew |
| `server/procCore.mjs` | getUsersList 等回傳時移除敏感欄位 |
| `src/components/LayoutContentUsers.vue` | 可選新增欄位顯示 |
| `server/procLang.mjs` | 新增欄位相關語系鍵 |
