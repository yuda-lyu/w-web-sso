# 規格書 5：使用者透過連結變更密碼

> 依賴：規格書 1（passwordPolicy）、規格書 2（密碼生成與驗證引擎）、規格書 3（使用者表結構擴充）、規格書 4（管理員重設密碼）
> 被依賴：無


## 一、目的

使用者收到管理員重設密碼的通知信後，點擊信中連結進入密碼變更頁面，輸入新密碼完成變更。變更成功後清除「待變更密碼」標記，恢復正常登入。


## 二、完整流程

```
使用者點擊信中連結
  → http://host/?view=resetPassword&resetToken=xxx
    → 前端偵測 URL 參數，顯示密碼重設頁面
      → 先呼叫 API：validateResetToken(resetToken)
        → 後端驗證 token 是否有效且未過期
          → 有效：前端顯示新密碼輸入表單
          → 無效/過期：前端顯示錯誤訊息
      → 使用者輸入新密碼 + 確認密碼
      → 前端驗證兩次輸入一致
      → 呼叫 API：submitResetPassword(resetToken, lang, newPassword)
        → 後端：
          1. 驗證 resetToken 有效且未過期
          2. 驗證新密碼符合 passwordPolicy
          3. hash 新密碼並儲存至資料庫
          4. 更新 timePasswordChanged
          5. 清除 isPendingPasswordReset = 'n'
          6. 清除 passwordResetToken、timePasswordResetExpired
          7. 寄送密碼變更成功通知 Email
        → 回傳成功
      → 前端顯示成功訊息，導向登入頁
```


## 三、後端實作

### 3.1 procCore.mjs — 新增 validateResetToken

**函式簽名：**
```javascript
let validateResetToken = async (resetToken) => { ... }
```

**邏輯：**
```
1. if (!isestr(resetToken)) → reject('invalid resetToken')

2. 查詢擁有此 token 的使用者：
   let users = await woItems.users.select({ passwordResetToken: resetToken })
   let user = users[0]
   if (!iseobj(user)) → reject('invalid resetToken')

3. 檢查是否過期：
   let expireTime = get(user, 'timePasswordResetExpired', '')
   if (!istimemsTZ(expireTime)) → reject('invalid resetToken')
   if (ot().isAfter(ot(expireTime))) → reject('resetToken expired')

4. 檢查帳號是否為待變更狀態：
   if (get(user, 'isPendingPasswordReset') !== 'y') → reject('invalid resetToken')

5. 回傳 { state: 'success', account: user.account }
   （回傳 account 供前端顯示「正在為 xxx 設定新密碼」）
```

### 3.2 procCore.mjs — 新增 submitResetPassword

**函式簽名：**
```javascript
let submitResetPassword = async (resetToken, lang, newPassword) => { ... }
```

**邏輯：**
```
1. 驗證 resetToken（同 validateResetToken 步驟 1-4）

2. 驗證新密碼格式：
   let r = validatePassword(newPassword, passwordPolicy)
   if (r.state === 'error') → reject(r.msg)

3. hash 新密碼：
   let hashedPw = hashPassword(newPassword, salt)

4. 儲存至資料庫：
   await woItems.users.save({
       id: user.id,
       password: hashedPw,
       isPendingPasswordReset: 'n',
       passwordResetToken: '',
       timePasswordResetExpired: '',
       timePasswordChanged: ot().format('YYYY-MM-DDTHH:mm:ss.SSSZ'),
   })

5. 寄送密碼變更成功通知 Email（non-blocking，共用現有 chpwEmTitle/chpwEmContent）

6. 回傳 { state: 'success' }
```

**匯出：** 兩個函式皆加入 return 的 `p` 物件

### 3.3 WWebSso.mjs — 新增 API

```javascript
validateResetToken: async (_t, resetToken) => {
    srLog.info({ event: 'kpfun-validateResetToken', resetToken })
    let r = await p.validateResetToken(resetToken)
    return r
},
submitResetPassword: async (_t, resetToken, lang, pwNew) => {
    srLog.info({ event: 'kpfun-submitResetPassword', resetToken, lang })
    let r = await p.submitResetPassword(resetToken, lang, pwNew)
    return r
},
```

> 注意：這兩個 API 為**公開 API**（不需登入 token），因為使用者此時處於無法登入狀態。以 resetToken 本身作為驗證手段。


## 四、前端實作

### 4.1 URL 路由偵測

現有前端透過 URL query 參數 `view` 決定顯示頁面（如 `?view=backstage`、`?view=user`）。

新增 `view=resetPassword` 的判斷：

**位置：** 需確認前端路由判斷的位置（應在 App.vue 或主要佈局元件中）

```javascript
// 偵測 URL 參數
let view = getQueryParam('view')
let resetToken = getQueryParam('resetToken')

if (view === 'resetPassword' && isestr(resetToken)) {
    // 顯示 PageResetPassword 元件
}
```

### 4.2 新增元件：PageResetPassword.vue

**UI 結構（參考 PageUser.vue 的樣式風格）：**

```
┌──────────────────────────────────────┐
│           🔒 重設密碼                  │
│                                      │
│  正在為帳號 ac-viewer 設定新密碼       │
│                                      │
│  新密碼                               │
│  ┌──────────────────────────────┐ 👁 │
│  │                              │    │
│  └──────────────────────────────┘    │
│  確認新密碼                           │
│  ┌──────────────────────────────┐ 👁 │
│  │                              │    │
│  └──────────────────────────────┘    │
│                                      │
│  [ 送出 ]  [ 取消 ]                  │
└──────────────────────────────────────┘
```

**元件狀態流程：**

```
mounted:
  → 呼叫 $fapi.validateResetToken(resetToken)
    → 成功：顯示密碼輸入表單，記錄 account
    → 失敗：顯示錯誤訊息（token 無效或已過期）

submitResetPassword:
  → 前端驗證：新密碼不為空、確認密碼一致
  → 呼叫 $fapi.checkUserPassword(lang, newPassword) 驗證格式
  → 呼叫 $fapi.submitResetPassword(resetToken, lang, newPassword)
    → 成功：顯示成功訊息，3 秒後導向登入頁
    → 失敗：顯示錯誤訊息
```

**data：**
```javascript
data: {
    resetToken: '',
    account: '',
    isValidToken: false,
    isTokenChecking: true,
    tokenError: '',
    newPassword: '',
    confirmPassword: '',
    showNewPassword: false,
    showConfirmPassword: false,
}
```

**components（複用現有元件）：**
- `WText` — 密碼輸入框
- `WButtonChip` — 送出/取消按鈕
- `WIcon` — 鎖頭圖示

**樣式：** 參考 PageLogin.vue 的頁面佈局（置中卡片），使用現有的背景漸層色。


## 五、語系

### procLang.mjs 新增

```javascript
resetPassword: {
    eng: 'Reset Password',
    cht: '重設密碼',
},
resetPasswordFor: {
    eng: 'Setting new password for account:',
    cht: '正在為帳號設定新密碼：',
},
resetPasswordNewPassword: {
    eng: 'New password',
    cht: '新密碼',
},
resetPasswordConfirmPassword: {
    eng: 'Confirm new password',
    cht: '確認新密碼',
},
resetPasswordForNoNewPassword: {
    eng: 'Please enter new password',
    cht: '尚未給予新密碼',
},
resetPasswordForNoConfirmPassword: {
    eng: 'Please enter confirm password',
    cht: '尚未給予確認密碼',
},
resetPasswordNotSame: {
    eng: 'New password and confirm password do not match',
    cht: '新密碼與確認密碼不一致',
},
resetPasswordSuccess: {
    eng: 'Password has been changed successfully. Redirecting to login page...',
    cht: '密碼變更成功，即將導向登入頁面...',
},
resetPasswordFail: {
    eng: 'Failed to reset password',
    cht: '密碼重設失敗',
},
resetPasswordTokenInvalid: {
    eng: 'This password reset link is invalid or has expired. Please contact the administrator.',
    cht: '此密碼重設連結無效或已過期，請聯絡系統管理員。',
},
resetPasswordTokenExpired: {
    eng: 'This password reset link has expired. Please contact the administrator to request a new one.',
    cht: '此密碼重設連結已過期，請聯絡系統管理員重新產生。',
},
```


## 六、安全性考量

| 項目 | 處理方式 |
|------|----------|
| resetToken 猜測攻擊 | token 為 UUID，熵值足夠 |
| token 過期機制 | 依 passwordPolicy.resetExpireHours 設定，過期後拒絕使用 |
| token 一次性使用 | 變更密碼成功後清除 token |
| API 無需登入 | validateResetToken、submitResetPassword 為公開 API，但以 resetToken 做驗證 |
| 密碼不記錄 log | srLog 僅記錄 resetToken 和 lang，不記錄 pwNew |
| 暴力破解防護 | 可考慮對 resetToken 驗證加入 rate limiting（複用現有 IP 限制機制） |


## 七、影響範圍

| 檔案 | 變動 |
|------|------|
| `server/procCore.mjs` | 新增 `validateResetToken`、`submitResetPassword` |
| `server/WWebSso.mjs` | 新增 2 個公開 API |
| `src/components/PageResetPassword.vue` | **新增**，密碼重設頁面 |
| 前端路由判斷處 | 新增 `view=resetPassword` 的路由判斷 |
| `server/procLang.mjs` | 新增語系鍵 |
