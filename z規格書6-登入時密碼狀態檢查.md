# 規格書 6：登入時密碼狀態檢查

> 依賴：規格書 3（使用者表結構擴充）
> 被依賴：規格書 7（密碼過期通知機制）


## 一、目的

在使用者登入流程中新增密碼狀態檢查，若偵測到以下情況則拒絕登入並回傳對應原因：
1. 帳號被標記為「待變更密碼」（`isPendingPasswordReset === 'y'`）
2. 密碼已過期（`timePasswordChanged` + `passwordPolicy.expireDays` < 當前時間）


## 二、現有登入流程

### 位置：procCore.mjs — loginByAccountAndPassword

```
現有流程：
1. 查詢帳號是否存在
2. 檢查帳號是否為 active
3. 比對密碼雜湊值
4. 檢查帳號是否被封鎖（timeBlocked）
5. 檢查帳號是否過期（timeExpired）
6. 檢查帳號是否已驗證（timeVerified）
7. 產生 token
8. 回傳登入成功
```

### 需插入的檢查點

在**步驟 3（密碼比對成功）之後、步驟 7（產生 token）之前**插入密碼狀態檢查。

理由：密碼比對必須先通過，確認身份無誤後，再告知密碼狀態問題。避免洩漏帳號是否存在的資訊。


## 三、檢查邏輯

### 3.1 待變更密碼檢查

```javascript
// 在密碼比對成功後
let isPending = get(user, 'isPendingPasswordReset', 'n')
if (isPending === 'y') {
    return Promise.reject({
        type: 'pendingPasswordReset',
        msg: get(kpLang, `${lang}.loginDeniedPendingPasswordReset`, ''),
    })
}
```

**注意：** 這裡密碼比對是不會成功的，因為管理員重設密碼時並未設定新密碼（只標記待變更）。但為了防禦性設計，仍需此檢查。實際上使用者在 `isPendingPasswordReset === 'y'` 狀態下，資料庫中的密碼是舊密碼（未被改動），使用者用舊密碼登入時會在此處被攔截。

### 3.2 密碼過期檢查

```javascript
// 僅在 passwordPolicy.expireDays > 0 時檢查
if (passwordPolicy.expireDays > 0) {
    let timePasswordChanged = get(user, 'timePasswordChanged', '')

    // 舊資料相容：若欄位不存在，以 timeCreate 作為基準
    if (!istimemsTZ(timePasswordChanged)) {
        timePasswordChanged = get(user, 'timeCreate', '')
    }

    if (istimemsTZ(timePasswordChanged)) {
        let expireTime = ot(timePasswordChanged).add(passwordPolicy.expireDays, 'day')
        if (ot().isAfter(expireTime)) {
            return Promise.reject({
                type: 'passwordExpired',
                msg: get(kpLang, `${lang}.loginDeniedPasswordExpired`, ''),
            })
        }
    }
}
```


## 四、前端處理

### 登入失敗回應處理

現有前端登入失敗統一顯示 `failedLoginForCatch` 錯誤訊息。需依據後端回傳的 `type` 欄位顯示不同訊息：

**位置：** PageLogin.vue 或前端登入處理邏輯

```javascript
// 登入 catch 處理
.catch((err) => {
    if (err && err.type === 'pendingPasswordReset') {
        // 顯示：您的密碼已被管理員重設，請至信箱收取重設連結
        vo.$alert(vo.$t('loginDeniedPendingPasswordReset'))
    }
    else if (err && err.type === 'passwordExpired') {
        // 顯示：您的密碼已過期，請至信箱收取密碼變更連結
        vo.$alert(vo.$t('loginDeniedPasswordExpired'))
    }
    else {
        vo.$alert(vo.$t('failedLoginForCatch'))
    }
})
```

> 備註：需確認前端 `$fapi` 的 catch 是否能完整傳遞後端 reject 的物件結構。若只傳遞字串，則需調整後端回傳格式或前端解析方式。


## 五、密碼過期時的自動處理

當密碼過期被偵測到時，除了拒絕登入外，可選擇是否同時：

**方案 A（推薦）：僅拒絕登入，提示使用者聯絡管理員**
- 簡單，不需額外邏輯
- 使用者需聯絡管理員執行「重設密碼」

**方案 B：自動觸發重設流程**
- 登入被拒時，後端自動標記 `isPendingPasswordReset = 'y'`，產生 resetToken，寄送 Email
- 使用者可自行透過信件連結變更密碼
- 較自動化，但需注意避免重複寄信（每次登入嘗試都觸發）

建議先實作**方案 A**，方案 B 可於規格書 7（密碼過期通知機制）中配合排程主動通知。


## 六、語系

### procLang.mjs 新增

```javascript
loginDeniedPendingPasswordReset: {
    eng: 'Your password has been reset by the administrator. Please check your email for the password reset link.',
    cht: '您的密碼已被管理員重設，請至信箱收取密碼重設連結。',
},
loginDeniedPasswordExpired: {
    eng: 'Your password has expired. Please contact the administrator to reset your password.',
    cht: '您的密碼已過期，請聯絡系統管理員重設密碼。',
},
```


## 七、passwordPolicy 傳遞

`loginByAccountAndPassword` 需要存取 `passwordPolicy.expireDays`。

此值已在規格書 1 中規劃由 WWebSso.mjs 傳入 procCore 的 opt 中，`loginByAccountAndPassword` 可直接使用 closure 中的 `passwordPolicy` 變數。


## 八、影響範圍

| 檔案 | 變動 |
|------|------|
| `server/procCore.mjs` | `loginByAccountAndPassword` 新增兩項密碼狀態檢查 |
| `src/components/PageLogin.vue` | 登入失敗處理新增分類型錯誤訊息 |
| `server/procLang.mjs` | 新增語系鍵 |


## 九、測試情境

| 情境 | 預期結果 |
|------|----------|
| 正常帳號登入 | 正常登入 |
| isPendingPasswordReset='y' 的帳號登入 | 拒絕，提示至信箱收取重設連結 |
| 密碼已過期的帳號登入 | 拒絕，提示密碼已過期 |
| expireDays=0 的設定下任何帳號登入 | 不檢查密碼過期 |
| 舊資料（無 timePasswordChanged 欄位）的帳號登入 | 以 timeCreate 為基準計算過期 |
